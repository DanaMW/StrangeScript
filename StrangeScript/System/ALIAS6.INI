[aliases]
n0=/msg {
n1=  if ($1 == $null) { $report(Msg,No destination sent).active | halt }
n2=  if ($2 == $null) { $report(Msg,No text sent).active | halt }
n3=  if (=* iswm $1) { msg $1- | return }
n4=  keywrite settings attempt.WHISPER 0
n5=  .raw privmsg $1 : $+ $2-
n6=  if ($show == $true) {
n7=    if ($chr(35) isin $1) && ($chan($1) != $null) { echo -t $1 $output  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $2- | return }
n8=    if ($chr(35) !isin $1) && ($query($1) != $null) { echo -t $1 $output  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $2- | return }
n9=    if ($chr(35) !isin $1) && ($query($1) == $null) { $report(Privmsg to $1,$null,--->,$2-).active }
n10=    return 
n11=  }
n12=}
n13=/aamsg { scid -a amsg $1- }
n14=/amsg {
n15=  if ($1 == $null) { $report(Msg,No text sent).active | halt }
n16=  if ($chan(0) < 1) { return }
n17=  var %amsg.count = 1
n18=  while (%amsg.count <= $chan(0)) {
n19=    .raw privmsg $chan(%amsg.count) : $+ $1-
n20=    if ($show == $true) { echo -t $chan(%amsg.count) $output  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $1- }
n21=    var %amsg = $addtok(%amsg,$chan(%amsg.count),44)
n22=    inc %amsg.count
n23=    if (%amsg.count > $chan(0)) { break }
n24=  }
n25=  return
n26=}
n27=/me {
n28=  if (=* iswm $active) { describe $active $1- | return }
n29=  .raw privmsg $active : $+ ACTION $1- $+ 
n30=  if ($show == $true) { echo $color(action) -t $active Action:  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $1- }
n31=  return
n32=}
n33=/aame { scid -a ame $1- }
n34=/ame {
n35=  var %n = 1
n36=  while (%n <= $chan(0)) {
n37=    .raw privmsg $chan(%n) : $+ ACTION $1- $+  
n38=    if ($show == $true) { echo $color(action) -t $chan(%n) Action:  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $1- }
n39=    inc %n
n40=    if (%n > $chan(0)) { break }
n41=  }
n42=  return
n43=}
n44=/spl {
n45=  if ($master(settings,remotecontrol.send) == ON) {
n46=    var %xXx = 0
n47=    while (%xXx <= 9) {
n48=      if (%remote.control [ $+ [ %xXx ] ] != $null) { msg $chr(61) $+ %remote.control [ $+ [ %xXx ] ] .cmd spl $1 }
n49=      inc %xXx
n50=      if (%xXx > 9) { break }
n51=    } 
n52=  }
n53=  if ($1 == OFF) { .timerSPL. $+ $network OFF | $report(Make Split,Split Timer,OFF).active | $report(Make Split,Final room count,%spl.count,rooms in,$duration($calc($ctime - %spl.start))).active | unset %spl.count %spl.start %splgo.count | return }
n54=  if ($1 == COUNT) { $report(Make Split,Current room count,%spl.count,rooms in,$duration($calc($ctime - %spl.start))).active | return }
n55=  if ($1 != $null) {
n56=    if ($1 !isnum) { $report(Make Split,$null,$null,The param must be a munber in seconds, between room joins,You sent ,$1).active | return }
n57=    if ($timer(spl. [ $+ [ $network ] ] ).com != $null) { .timerSPL. $+ $network 0 $1 splgo | $report(Make Split,Split Timer,Active,Updating to,$1, seconds).active | return }
n58=    if ($timer(spl. [ $+ [ $network ] ] ).com == $null) { set %spl.count 0 | set %spl.start $ctime | .timerSPL. $+ $network 0 $1 splgo | $report(Make Split,Split Timer,Activated,Using delay,$1, seconds).active | return }
n59=  }
n60=  else {
n61=    if ($timer(spl. [ $+ [ $network ] ] ).com != $null) { $report(Make Split,Split timer,Active,With a delay of,$timer(spl. [ $+ [ $network ] ] ).delay).active }
n62=    if ($timer(spl. [ $+ [ $network ] ] ).com == $null) { $report(Make Split,$null,$null,$null,Split timer is not active).active }
n63=    $report(Make Split,$null,$null,You must include "seconds between sends" on the command line.).active
n64=    return
n65=  }
n66=}
n67=/saysplit { msg # $report(Split Channels,$null,$null,$null,%split.chan) }
n68=/splgo {
n69=  var %tmp11 1
n70=  while (%tmp11 <= $sock(*,0)) {
n71=    if (@Boom isin $sock(*,%tmp11)) || (@Gate isin $sock(*,%tmp11)) || (@Split isin $sock(*,%tmp11)) {
n72=      var %test $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999)
n73=      sockwrite -n $sock(*,%tmp11) join %test
n74=      sockwrite -n $sock(*,%tmp11) part %test
n75=      inc %spl.count 10
n76=    }
n77=    inc %tmp11
n78=    if (%tmp11 > $sock(*,0)) { break }
n79=  }
n80=  $report(Make Split,%spl.count,$duration($calc($ctime - %spl.start)),Per Minute,$round($calc(%spl.count / (($ctime - %spl.start) / 60)),0)).status
n81=}
n82=/newkey return StrangeScript $+ $chr(91) $+ $rand(0,99999) $+ $rand(A,Z) $+ $rand(A,Z) $+ $rand(0,99999) $+ $chr(93)
n83=/addkey {
n84=  if ($key(settings,rumble) == ON) { return }
n85=  ;if ($key(settings,ircx) == OFF) { return }
n86=  if ($1 == $null) {
n87=    if ($key(#,deop) == ON) { .raw prop # ownerkey : $+ $key(#,ownerkey)  }
n88=    elseif ($key(#,deop) == PROTECT) { .raw prop # ownerkey : $+ $key(#,ownerkey)  }
n89=    else { keywrite # ownerkey $newkey | .raw prop # ownerkey : $+ $key(#,ownerkey) }
n90=  }
n91=  if ($1 != $null) {
n92=    if ($key($1,deop) == ON) { .raw prop $1 ownerkey : $+ $key($1,ownerkey)  }
n93=    elseif ($key($1,deop) == PROTECT) { .raw prop $1 ownerkey : $+ $key($1,ownerkey)  }
n94=    else { keywrite $1 ownerkey $newkey | .raw prop $1 ownerkey : $+ $key($1,ownerkey) }
n95=  }
n96=  return
n97=}
n98=/addme {
n99=  if ($key(settings,rumble) == ON) { return }
n100=  if ($key(settings,ircx) == OFF) { return }
n101=  if ($1 == $null) { .raw access # add owner $address($me,4) 0 : $+ $ver }
n102=  else {
n103=    if ($1 == all) { addme.all }
n104=    else { .raw access $1 add owner $address($me,4) 0 : $+ $ver }
n105=  }
n106=}
n107=/addme2 {
n108=  if ($key(settings,rumble) == ON) { return }
n109=  if ($key(settings,ircx) == OFF) { return }
n110=  if ($1 == $null) { .raw access # add host $address($me,4) 0 : $+ $ver }
n111=  else {
n112=    if ($1 == all) { addme.all }
n113=    else { .raw access $1 add host $address($me,4) 0 : $+ $ver }
n114=  }
n115=}
n116=/addme.all {
n117=  if ($key(settings,rumble) == ON) { return }
n118=  if ($key(settings,ircx) == OFF) { return }
n119=  var %tmp. 1
n120=  while (%tmp.adjust <= $chan(0)) {
n121=    if ($nick($chan(%tmp.adjust),$me,q) != $null) { addme $chan(%tmp.adjust) }
n122=    if ($nick($chan(%tmp.adjust),$me,o) != $null) { addme2 $chan(%tmp.adjust) }
n123=    inc %tmp.adjust
n124=    if (%tmp.adjust > $chan(0)) { break }
n125=  }
n126=}
n127=/cyc {
n128=  if ($1 == OFF) { timerCYC off | return }
n129=  if ($1 == $null) {
n130=    var %tmp.cyc $$?="Seconds between sends"
n131=    var %tmp.cyc1 %tmp.cyc $?!="Do you want it to cycle?"
n132=    var %tmp.cyc3 $?="What Channel? $+ $cr $+ OK for Current"
n133=    if (%tmp.cyc3 == $null) { var %tmp.cyc3 = # }
n134=    timerCYC 0 %tmp.cyc cyc %tmp.cyc1 %tmp.cyc3
n135=  }
n136=  if ($1 != $null) {
n137=    if ($2 == $true) {
n138=      var %tmp.cf 1
n139=      while (%tmp.cf <= $sock(*,0)) {
n140=        if ( @Boom isin $sock(*,%tmp.cf)) || (@Gate isin $sock(*,%tmp.cf)) || (@Split isin $sock(*,%tmp.cf)) { sockwrite -n $sock(*,%tmp.cf) join $3 $+ $cr $+ privmsg $3 : $+  $+ $rand(0,15) $+ $str(XoX,130) $+ $cr $+ part $3 }
n141=        inc %tmp.cf
n142=        if (%tmp.cf > $sock(*,0)) { break }
n143=      }
n144=    }
n145=  }
n146=  if ($2 == $false) {
n147=    var %tmp.cf 1
n148=    while (%tmp.cf <= $sock(*,0)) {
n149=      sockwrite -n $sock(*,%tmp.cf) privmsg $3 : $+  $+ $rand(0,15) $+ $str(SeX,130)
n150=      inc %tmp.cf
n151=      if (%tmp.cf > $sock(*,0)) { break }
n152=    }
n153=  }
n154=}
n155=/boot { raw mode # -o-o-o-o-o-o $1 $1 $1 $1 $1 $1 $+ $cr $+ kick # $1 SpakkerHead }
n156=/blitz { .raw part # $crlf join # $crlf part # $crlf join # $crlf part # $crlf join # $crlf part # $crlf join # $key(#,ownerkey) }
n157=sysop.menu { return SysOp Menu }
n158=/dos { if ($OS == XP) || ($os == 2K) { run cmd.exe } | else { run command.com } }
n159=/telnet {
n160=  if ($1 == $null) {
n161=    var %ttmp = $input(IP to telnet to?,egi,StrangeScript needs input,127.0.0.1)
n162=    if (%ttmp == $null) { halt }
n163=    run telnet %ttmp
n164=  }
n165=  else { run telnet $1- }
n166=}
n167=/http { www $1- }
n168=/www {
n169=  ;if ($1 == $null) {
n170=  ;  var %htmp = $input(IP to telnet to?,egi,StrangeScript needs input,127.0.0.1)
n171=  ;  if (%htmp == $null) { halt }
n172=  ;  run %htmp
n173=  ;}
n174=  if (http* !isin $1-) {
n175=    if (*www* !isin $1-) { run http://www. $+ $1- } 
n176=    { run http:// $+ $1- | halt }
n177=  }
n178=}
n179=/ftp {
n180=  if ($1 == $null) {
n181=    var %ttmp = $input(IP to ftp to?,egi,StrangeScript needs input,127.0.0.1)
n182=    if (%ttmp == $null) { halt }
n183=    run ftp %ttmp
n184=  }
n185=  else { run ftp $1- }
n186=}
n187=/udp.flood { sockudp -nk * $1 $rand(1,5000) ||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||| }
n188=/script.play {
n189=  if ($master(settings,script.sound) != ON) { return }
n190=  if ($right($1-,3) == wav) {
n191=    if ($inwave != $true) { splay -w $1- }
n192=  }
n193=  if ($right($1-,3) == mp3) || ($right($1-,3) == wma) {
n194=    if ($insong != $true) { splay -p $1- }
n195=  }
n196=  ;$report(Playing a *. $+ $upper($right($1,3)) sound file).status
n197=  return
n198=}
n199=/unshit {
n200=  keywrite settings shithead
n201=  keywrite settings shitheadlast
n202=  rumble OFF
n203=  $report(ShitList,CLEAR,$null,The list has been cleared).active
n204=  if ($key(settings,shithead) == $null) { $report(ShitList,SHOW,$null,The current Shit List is EMPTY).active }
n205=  if ($key(settings,shithead) != $null) { $report(ShitList,SHOW,$null,The current Shit List is $replace($key(settings,shithead),$chr(44),$chr(160))).active }
n206=}
n207=/shit {
n208=  if ($key(settings,shitheadlast) == $null) { 
n209=    $report(Last ShitList,EMPTY,$null,No action taken).active
n210=    if ($key(settings,shithead) == $null) { $report(ShitList,SHOW,$null,The current Shit List is EMPTY).active }
n211=    if ($key(settings,shithead) != $null) { $report(ShitList,SHOW,$null,The current Shit List is $replace($key(settings,shithead),$chr(44),$chr(160))).active }
n212=    rumble OFF
n213=    return
n214=  }
n215=  if ($key(settings,shitheadlast) != $null) { 
n216=    keywrite settings shithead $remtok($key(settings,shithead),$key(settings,shitheadlast),1,44)
n217=    $report(Last ShitList,REMOVE,$null,The ip $key(settings,shitheadlast) was removed).active
n218=    if ($key(settings,shithead) == $null) { $report(ShitList,SHOW,$null,The current Shit List is EMPTY).active }
n219=    if ($key(settings,shithead) != $null) { $report(ShitList,SHOW,$null,The current Shit List is $replace($key(settings,shithead),$chr(44),$chr(160))).active }
n220=    keywrite settings shitheadlast
n221=    rumble OFF
n222=    return
n223=  }
n224=}
n225=/split.flood {
n226=  if ($1 == $null) { $report(ERROR in Split.Flood).active | return }
n227=  set %tmp.splitflood 1
n228=  while (%tmp.splitflood <= $sock(*,0)) {
n229=    if (%tmp.splitflood.tic == ON) { set %tmp.splitflood.tic OFF }
n230=    else { set %tmp.splitflood.tic ON }
n231=    if (%tmp.splitflood.tic == OFF) { set %tmp.splitflood.say mode $1 +ntsuw }
n232=    else { set %tmp.splitflood.say mode $1 -ntuw }
n233=    sockwrite -n $sock(*,%tmp.splitflood) %tmp.splitflood.say
n234=    inc %tmp.splitflood
n235=    if (%tmp.splitflood > $sock(*,0)) { break }
n236=  }
n237=  unset %tmp.splitflood %tmp.splitflood
n238=}
n239=/link {
n240=  if ($1 == $null) { set %tmp.link $$?="Nick or IP to link with" }
n241=  if ($1 == $null) && (%tmp.link == $null) { return }
n242=  if ($1 != $null) { set %tmp.link $1- }
n243=  ctcp %tmp.link MultiLink $$?="Password for Link"
n244=  unset %tmp.link
n245=}
n246=/RC.Clear { unset %remote.control* | setupshow RC nicks ALL cleared. | return }
n247=/nick.check {
n248=  if ($key(settings,beme) == ON) {
n249=    if ($key(settings,savednick1) != $null) && ($me != $key(settings,savednick1)) { if ($key(settings,recover) == $null) { recover $key(settings,savednick1) | return } }
n250=  }
n251=  return
n252=}
n253=/launch {
n254=  boom
n255=  .timer 1 30 gate
n256=}
n257=/alt.fix {
n258=  if ($1 != ALL) {
n259=    if ($1 !isnum) { return }
n260=  }
n261=  if ($1 == ALL) {
n262=    if ($1 isnum) { return }
n263=  }
n264=}
n265=/fix {
n266=  if ($1 == $null) {
n267=    $report(Fix,Commands,/FIX B ## or /FIX G ## or /FIX ALL).active
n268=    $report(Fix,Commands,B is Boom G is Gate, Soon you will be able to use ranges, /FIX B 2-5).active
n269=  }
n270=  if ($1 != $null) {
n271=    if ($1 == ALL) {
n272=      $report(Fix,$null,$null,Closing all Boom and Gate Remote Connections).active
n273=      if (status !isin $window($active)) { $report(Fix,$null,$null,Closing all Boom and Gate Remote Connections).status }
n274=      sockclose @Boom*
n275=      sockclose @Gate*
n276=      keywrite settings boom.sock.chan
n277=      keywrite settings boom.sock.nick
n278=      keywrite settings clone.sock.chan
n279=      keywrite settings clone.sock.nick
n280=      unset %boom.server*
n281=      unset %clone.server*
n282=      var %tmp = 1
n283=      while (%tmp <= 999) {
n284=        window -c @Boom [ $+ [ %tmp ] ]
n285=        window -c @Gate [ $+ [ %tmp ] ]
n286=        inc %tmp
n287=        if (%tmp > 999) { break }
n288=      }
n289=      return
n290=    }
n291=    if (Boom isin $1) {
n292=      var %tfix1 = B
n293=      var %tfix2 = $remove($1,@Boom)
n294=      goto setboom
n295=    }
n296=    var %tfix1 = $1
n297=    if ($2 == $null) { $report(Fix,Commands,/FIX B ## or /FIX G ## or /FIX ALL).active | return }
n298=    if ($chr(45) isin $2) { $report($null,$null,$null,$null,Ranges are not operational yet.).active | return }
n299=    var %tfix2 = $2
n300=    :setboom
n301=    if (B isin $1) { var %tfix1 = @Boom }
n302=    if (G isin $1) { var %tfix1 = @Gate }
n303=    var %tfix1 = %tfix1 $+ %tfix2
n304=    $report(Fix,$null,Remote Connection,%tfix1,Closing window and resetting variables).active
n305=    if (status !isin $window($active)) { $report(Fix,$null,Remote Connection,%tfix1,Closing window and resetting variables).status }
n306=    sockclose %tfix1
n307=    window -c %tfix1
n308=    if (@Boom !isin %tfix1) {
n309=      keywrite settings boom.sock.chan
n310=      keywrite settings boom.sock.nick
n311=      unset %boom.server [ $+ [ %tfix1 ] ]
n312=    }
n313=    if (@Gate isin %tfix1) {
n314=      keywrite settings clone.sock.chan
n315=      keywrite settings clone.sock.nick
n316=      unset %clone.server [ $+ [ %tfix1 ] ]
n317=    }
n318=    if ($master(settings,remotecontrol.send) == ON) {
n319=      var %xXx = 0
n320=      while (%xXx <= 9) {
n321=        if (%remote.control [ $+ [ %xXx ] ] != $null) { msg $chr(61) $+ %remote.control [ $+ [ %xXx ] ] .cmd fix %tfix1 }
n322=        inc %xXx
n323=        if (%xXx > 9) { break }
n324=      } 
n325=    }
n326=    return
n327=  }
n328=  return
n329=}
n330=/boom {
n331=  var %tmp.1 $1 , %tmp.2 $2 , %tmp.3 $3
n332=  ;if (%tmp.1 != $null) && (%tmp.1 !isnum) && (%tmp.2 == $null) { var %tmp.1 2 | var %tmp.2 $network }
n333=  if (%tmp.1 == $null) { var %tmp.1 2 }
n334=  if (%tmp.2 == $null) { var %tmp.2 $bAdd }
n335=  if (%tmp.2 == $null) && (%tmp.2 == $null) { var %tmp.2 $serverip }
n336=  if (%tmp.3 == $null) { var %tmp.3 6667 }
n337=  var %tmp.boom.count 1
n338=  while (%tmp.boom.count <= %tmp.1) {
n339=    if ($sock(@Boom [ $+ [ %tmp.boom.count ] ] ).name == $null) { sockopen @Boom [ $+ [ %tmp.boom.count ] ] %tmp.2 %tmp.3 }
n340=    inc %tmp.boom.count
n341=    if (%tmp.boom.count > %tmp.1) { break }
n342=  }
n343=  if ($master(settings,remotecontrol.send) == ON) {
n344=    var %xXx 0
n345=    while (%xXx <= 9) {
n346=      if (%remote.control [ $+ [ %xXx ] ] != $null) { msg $chr(61) $+ %remote.control [ $+ [ %xXx ] ] .cmd boom %tmp.1 }
n347=      inc %xXx
n348=      if (%xXx > 9) { break }
n349=    } 
n350=  }
n351=  return
n352=}
n353=/Gate.alt {
n354=  set %gate.load $read(Gates.txt,n,$1)
n355=  var %cloneload 1
n356=  $report(Begining @Gate socket load of line $1 %gate.load).active
n357=  ;while (%cloneload <= 1) {
n358=  if ($sock(@Gate [ $+ [ %CloneNum ] ] ).name != $null) { inc %CloneNum }
n359=  if ($sock(@Gate [ $+ [ %CloneNum ] ] ).name == $null) { sockopen @Gate $+ %CloneNum %gate.load 23 }
n360=  inc %CloneNum
n361=  if ($sock(@Gate [ $+ [ %CloneNum ] ] ).name == $null) { sockopen @Gate $+ %CloneNum %gate.load 1080 }
n362=  inc %CloneNum
n363=  if ($sock(@Gate [ $+ [ %CloneNum ] ] ).name == $null) { sockopen @Gate $+ %CloneNum %gate.load 80 }
n364=  inc %CloneNum
n365=  if (%CloneNum > 999) { set %CloneNum 1 }
n366=  inc %cloneload
n367=  if (%cloneload > 1) { break }
n368=  ;}
n369=  $report(Done,$null,$null,Please wait for sockets to load).active
n370=}
n371=/gate {
n372=  ;set %bah.load $lines(Gates.txt)
n373=  if ($1 == $null) { set %CloneNum 1 | var %gatenext 1, %gateloop = $calc(%gatenext +9) }
n374=  if ($1 != $null) { var %gatenext $1 | var %gateloop $calc(%gatenext +9) }
n375=  :next
n376=  while (%gatenext <= %gateloop) {
n377=    .timergate $+ %gatenext 1 $calc(%gatenext * 4) gate.alt %gatenext
n378=    inc %gatenext
n379=    if (%gatenext > %gateloop) { break }
n380=    if (%gatenext > $lines(Gates.txt)) { break }
n381=  }
n382=  set %CloneNum1
n383=  return
n384=}
n385=Parsme {
n386=  if ($window(@NumParse) == $null) { window -l @NumParse 0 408 800 100 @NumParse Arial 12 bold }
n387=  var %pi = 1
n388=  while ($gettok(%Parse,%pi,32) != $null) {
n389=    var %Parsed = %Parsed 10 $+ $chr(91) $+ 04Param $+ %pi $+ : 09 $+ $gettok(%Parse,%pi,32) $+ 10 $+ $chr(93)
n390=    inc %pi
n391=    if ($gettok(%Parse,%pi,32) == $null) { break }
n392=  }
n393=  aline @NumParse %ParseNum %Parsed 
n394=  sline @NumParse $line(@NumParse,0)
n395=}
n396=/dping {
n397=  if ($1 == $null) { run ping -t -l 65500 -v 8 -w 100 $$?="Enter IP to flood:" }
n398=  else { run ping -t -l 65500 -v 8 -w 100 $1 }
n399=}
n400=/end {
n401=  if ($1 == $null) { $report(No range given. /end 1 2 3 or /end 1-21).active | return }
n402=  set %tmp.end $1-
n403=  if ($chr(45) isin %tmp.end) { goto range }
n404=  set %tmp.end.count 1
n405=  while (%tmp.end.count <= $numtok(%tmp.end,32)) {
n406=    stop $gettok(%tmp.end,%tmp.end.count,32)
n407=    inc %tmp.end.count
n408=    if (%tmp.end.count > $numtok(%tmp.end,32)) { break }
n409=  }
n410=  unset %tmp.end %tmp.end.count
n411=  return
n412=  :range
n413=  set %tmp.end $gettok(%tmp.end,1,32)
n414=  set %tmp.end.count $remove($left(%tmp.end,$pos(%tmp.end,-,1)),-)
n415=  while (%tmp.end.count <= $remove($right(%tmp.end,$pos(%tmp.end,-,1)),-)) {
n416=    stop %tmp.end.count
n417=    inc %tmp.end.count
n418=    if (%tmp.end.count > $remove($right(%tmp.end,$pos(%tmp.end,-,1)),-)) { break }
n419=  }
n420=  unset %tmp.end %tmp.end.count
n421=  if ($numtok($1-,32) > 1) { .timer 1 0 end $2- }
n422=}
n423=/so { msg # $report(Sockets's Open,$null,$sock(*,0),$null,$sock(@Boom*,0), Booms, $sock(@Gate*,0), Gates,$sock(@Split*,0), Splits,$calc($sock(*,0) - ($sock(@Boom*,0) + $sock(@Gate*,0) + $sock(@Split*,0))), others) }
n424=/insta.aj {
n425=  $report(Insta-AutoJoin,$null,$null,Creating your autojoin list).active
n426=  var %tmp.iaj = 1
n427=  var %tmp.iaj1 = ""
n428=  while (%tmp.iaj <= $chan(0)) {
n429=    var %tmp.iaj1 = $addtok(%tmp.iaj1,$chan(%tmp.iaj),44)
n430=    $report(Insta-AutoJoin,$null,Adding,$null,$chan(%tmp.iaj)).active
n431=    inc %tmp.iaj
n432=    if (%tmp.iaj > $chan(0)) { break }
n433=  }
n434=  keywrite settings autojoinrooms %tmp.iaj1
n435=  $report(Insta-AutoJoin,$null,Done,%tmp.iaj1).active
n436=}
n437=/reup { set %split.dc off | server internal $1 }
n438=/kall {
n439=  var %tmp.kall = 1
n440=  set %tmp.kst ""
n441=  while (%tmp.kall <= $ialchan($1,#,0)) {
n442=    if ($ialchan($1,#,%tmp.kall).nick != $me) { set %tmp.kst $addtok(%tmp.kst,$ialchan($1,#,%tmp.kall).nick,44) }
n443=    inc %tmp.kall
n444=    if (%tmp.kall > $ialchan($1,#,0)) { break }
n445=  }
n446=  if ($2 == BAN) {
n447=    if ($key(settings,rumble) == ON) { .access # add deny $1 0 : $+ $ver }
n448=    else { .ban # $1 }
n449=    kick # %tmp.kst $1 $+ $chr(160) $+ $chr(160) $+ 10 $+ ( $+ 00IP Ban $+ 10 $+ )
n450=  }
n451=  else { .kick # %tmp.kst $1 $+ $chr(160) $+ 10 $+ ( $+ 00IP Kick $+ 10 $+ ) }
n452=  unset %tmp.kst
n453=  return
n454=}
n455=/adjust {
n456=  if ($key(settings,rumble) == ON) { return }
n457=  clearial
n458=  var %tmp.adjust = 1
n459=  while (%tmp.adjust <= $chan(0)) {
n460=    if ($me isop $chan(%tmp.adjust)) { .who $chan(%tmp.adjust) }
n461=    ;if ($me isop $chan(%tmp.adjust)) && ($nick($chan(%tmp.adjust),0) < 200) { .who $chan(%tmp.adjust) }
n462=    inc %tmp.adjust
n463=    if (%tmp.adjust > $chan(0)) { break }
n464=  }
n465=  $report(IAL,$null,Auto,Updating,All Op Channels).status
n466=  ;.timerlogcolor. $+ $network 1 10 login.color.nick
n467=  .timerMASTER. $+ $network 1 10 auto.master
n468=}
n469=/sl { socklist }
n470=/sc { sockclose $1- }
n471=weapons { if ($exists($mircdirwar) == $true) { return Weapons } }
n472=/scan {
n473=  if ($1 == $me) { halt }
n474=  if ($1 == $null) { $report(Please include the nick and IP /scan <nick> <IP>).active | halt }
n475=  if ($2 == $null) { $report(Please include the nick and IP /scan <nick> <IP>).active | halt }
n476=  if ($chr(64) isin $2) {  
n477=    set %Gate.Usera [ $+ [ %Gate.Scaning.count ] ] $1 $gettok($2,2,64)
n478=    sockopen @sockchecka [ $+ [ %Gate.Scaning.count ] ] $gettok($2,2,64) 1080
n479=    set %Gate.Userb [ $+ [ %Gate.Scaning.count ] ] $1 $gettok($2,2,64)
n480=    sockopen @sockcheckb [ $+ [ %Gate.Scaning.count ] ] $gettok($2,2,64) 23
n481=  }
n482=  if ($chr(64) !isin $2) {  
n483=    set %Gate.Usera [ $+ [ %Gate.Scaning.count ] ] $1 $2
n484=    sockopen @sockchecka [ $+ [ %Gate.Scaning.count ] ] $2 1080
n485=    set %Gate.Userb [ $+ [ %Gate.Scaning.count ] ] $1 $2
n486=    sockopen @sockcheckb [ $+ [ %Gate.Scaning.count ] ] $2 23
n487=  }
n488=  .timerGATEAC $+ %Gate.Scaning.count 1 30 /sockclose @sockchecka [ $+ [ %Gate.Scaning.count ] ]
n489=  .timerGATEAV $+ %Gate.Scaning.count 1 30 /unset %Gate.Usera [ $+ [ %Gate.Scaning.count ] ]
n490=  .timerGATEBC $+ %Gate.Scaning.count 1 30 /sockclose @sockcheckb [ $+ [ %Gate.Scaning.count ] ]
n491=  .timerGATEBV $+ %Gate.Scaning.count 1 30 /unset %Gate.Userb [ $+ [ %Gate.Scaning.count ] ]
n492=  inc %Gate.Scaning.count
n493=  if (%Gate.Scaning.count > 999) { set %Gate.Scaning.count 0 | halt }
n494=}
n495=/system { msg # $report(System Info,Windows $os) $report($null,CPU: 1400 mhz) }
n496=/unkill { keywrite settings killonops.chan | keywrite settings killonops.nick | $report(Kill,$null,Cleared,$null,Your kill alias has been cleared).active }
n497=custom.filter {
n498=  if ($gettok(%custom.filter,1,44) != $null) && (* $+ $gettok(%custom.filter,1,44) $+ * iswm $strip($1-)) { return $true }
n499=  elseif ($gettok(%custom.filter,2,44) != $null) && (* $+ $gettok(%custom.filter,2,44) $+ * iswm $strip($1-)) { return $true }
n500=  elseif ($gettok(%custom.filter,3,44) != $null) && (* $+ $gettok(%custom.filter,3,44) $+ * iswm $strip($1-)) { return $true }
n501=  elseif ($gettok(%custom.filter,4,44) != $null) && (* $+ $gettok(%custom.filter,4,44) $+ * iswm $strip($1-)) { return $true }
n502=  elseif ($gettok(%custom.filter,5,44) != $null) && (* $+ $gettok(%custom.filter,5,44) $+ * iswm $strip($1-)) { return $true }
n503=  elseif ($gettok(%custom.filter,6,44) != $null) && (* $+ $gettok(%custom.filter,6,44) $+ * iswm $strip($1-)) { return $true }
n504=  elseif ($gettok(%custom.filter,7,44) != $null) && (* $+ $gettok(%custom.filter,7,44) $+ * iswm $strip($1-)) { return $true }
n505=  elseif ($gettok(%custom.filter,8,44) != $null) && (* $+ $gettok(%custom.filter,8,44) $+ * iswm $strip($1-)) { return $true }
n506=  elseif ($gettok(%custom.filter,9,44) != $null) && (* $+ $gettok(%custom.filter,9,44) $+ * iswm $strip($1-)) { return $true }
n507=  else { return $false }
n508=}
n509=ChitChat {
n510=  if ($key(settings,chitchat) == ON) {
n511=    if ($window(@ChitChat) == $null) { window @ChitChat @ChitChat }
n512=    echo -t @ChitChat On $network in $2 $+ , $1 said: $3-
n513=  }
n514=  return
n515=}
n516=/bnc {
n517=  if ($1 != $null) { masterwrite settings bnc.server $1- }
n518=  if ($master(settings,bnc.ip) == $null) { masterwrite settings bnc.ip $input(Enter the BNC IP you want  to connect to,egi,StrangeScript needs input,127.0.0.1) }
n519=  if ($master(settings,bnc.port) == $null) { masterwrite settings bnc.port $input(Enter the BNC Port to connect on,egi,StrangeScript needs input,6667) }
n520=  if ($master(settings,bnc.vhost) == $null) { masterwrite settings bnc.vhost $input(Enter the VHost to use,egi,StrangeScript needs input) }
n521=  if ($master(settings,bnc.keepalive) == $null) { masterwrite settings bnc.keepalive $input(Enable keepalive?,ygi,StrangeScript needs input) }
n522=  if ($master(settings,bnc.pass) == $null) { masterwrite settings bnc.pass $input(Enter the BNC password,pgi,StrangeScript needs input) }
n523=  if ($master(settings,bnc.server) == $null) { masterwrite settings bnc.server $input(Enter the server to head to with the BNC,egi,StrangeScript needs input,127.0.0.1) }
n524=  server $master(settings,bnc.ip) $master(settings,bnc.port)
n525=  $report(Use /bnc2 to send codes and connect info).active
n526=}
n527=/bnc2 {
n528=  if ($master(settings,bnc.use) != ON) { return }
n529=  raw pass $master(settings,bnc.pass)
n530=  if ($master(settings,bnc.keepalive) == $true) { raw keepalive }
n531=  raw vip $master(settings,bnc.vhost)
n532=  raw conn $master(settings,bnc.server)
n533=}
n534=/BNC.Server {
n535=  set %BNC.version StrangeBNC v 1.6
n536=  if ($1 == INFO) {
n537=    var %tmp.b = $sock(BNCUSER*,0)
n538=    if (%tmp.b == $null) { var %tmp.b = 0 }
n539=    $report(BNC,INFO,$null,$null,Listing Information for %BNC.version).active
n540=    $report(BNC,INFO,$null,$null,There is currently %tmp.b connection(s) to the BNC server.).active
n541=    var %tmp.bnci = 1
n542=    while (%tmp.bnci <= $sock(BNCUSER*,0)) {
n543=      $report(BNC,INFO,User %tmp.bnci,%BNC.nick [ $+ [ $right($sock(BNCUSER*,%tmp.bnci),-7) ] ] ,%BNC.user [ $+ [ $right($sock(BNCUSER*,%tmp.bnci),-7) ] ] ).active
n544=      $report(BNC,INFO,User %tmp.bnci,Connected to %BNC.server [ $+ [ $right($sock(BNCUSER*,%tmp.bnci),-7) ] ] ).active
n545=      inc %tmp.bnci
n546=      if (%tmp.bnci > $sock(BNCUSER*,0)) { break }
n547=    }
n548=    return
n549=  }
n550=  if ($master(settings,Sbnc.active) == $null) { masterwrite settings Sbnc.active OFF | setupshow BNC Server offline }
n551=  if ($master(settings,Sbnc.port) == $null) { masterwrite settings Sbnc.port 1000 | setupshow BNC Port $master(settings,Sbnc.port) }
n552=  if ($master(settings,Sbnc.active) == ON) { if ($sock(BNCS) == $null) { socklisten BNCS $master(settings,Sbnc.port) } | set %BNC.count 1 }
n553=  if ($master(settings,Sbnc.active) == OFF) { sockclose BNCS* | sockclose BNCUSER* | sockclose BNCRELAY* | unset %BNC.nick* %BNC.user* %BNC.pass* %BNC.authed* %BNC.level* }
n554=  return
n555=}
n556=;/quote {
n557=;  if ($1 == 1) { quote.text $2- | halt }
n558=;  if ($1 == 2) { quote.clip $2- | halt }
n559=;  else {
n560=;    $report(Quote,$null,Usage:,/quote 1 [Reply to the last chatter]).active
n561=;    $report(Quote,$null,If you don't include a reply on the command line it will ask you for it.).active
n562=;    $report(Quote,$null,Usage:,/quote 2 [Reply to the clipboard contents]).active
n563=;    $report(Quote,$null,Agian if you don't include a reply on the command line it will ask you for it.).active
n564=;    ;quote.text $2-
n565=;    halt
n566=;  }
n567=;}
n568=;/quote.text {
n569=;  if ($1 != $null) { say $report($null,$null,$chr(126) $+ $token(%quote.text. [ $+ [ $network ] ] ,3,46) $+ $chr(126)) 1 $1- | halt }
n570=;  if (%quote.text. [ $+ [ $network ] ] != $null) { say $report($null,$null,$chr(126) $+ $token(%quote.text. [ $+ [ $network ] ] ,3,46) $+ $chr(126)) 1 $$?="Text after quote:" | halt }
n571=;  else { $report($null,Error,$null,quote.text Failed,Buffer Empty).active }
n572=;}
n573=;/quote.clip {
n574=;  if ($1 != $null) { say $report($null,$null,$chr(126) $+ $cb $+ $chr(126)) 1 $1- | halt }
n575=;  elseif ($cb != $null) { say $report($null,$null,$chr(126) $+ $cb $+ $chr(126)) 1 $$?="Text after quote:" | halt }
n576=;  else { $report($null,Error,$null,quote.clip failed,Buffer Empty).active }
n577=;}
