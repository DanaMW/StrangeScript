[aliases]
n0=/msg {
n1=  if ($1 == $null) { $report(Msg,No destination sent).active | halt }
n2=  if ($2 == $null) { $report(Msg,No text sent).active | halt }
n3=  if (=* iswm $1) { msg $1- | return }
n4=  keywrite settings attempt.WHISPER 0
n5=  .raw privmsg $1 : $+ $2-
n6=  if ($show == $true) {
n7=    if ($chr(35) isin $1) && ($chan($1) != $null) { echo -t $1 $output  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $2- | return }
n8=    if ($chr(35) !isin $1) && ($query($1) != $null) { echo -t $1 $output  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $2- | return }
n9=    if ($chr(35) !isin $1) && ($query($1) == $null) { $report(Privmsg to $1,$null,--->,$2-).active }
n10=    return 
n11=  }
n12=}
n13=/aamsg { scid -a amsg $1- }
n14=/amsg {
n15=  if ($1 == $null) { $report(Msg,No text sent).active | halt }
n16=  if ($chan(0) < 1) { return }
n17=  var %amsg.count = 1
n18=  while (%amsg.count <= $chan(0)) {
n19=    .raw privmsg $chan(%amsg.count) : $+ $1-
n20=    if ($show == $true) { echo -t $chan(%amsg.count) $output  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $1- }
n21=    var %amsg = $addtok(%amsg,$chan(%amsg.count),44)
n22=    inc %amsg.count
n23=    if (%amsg.count > $chan(0)) { break }
n24=  }
n25=  return
n26=}
n27=/me {
n28=  if (=* iswm $active) { describe $active $1- | return }
n29=  .raw privmsg $active : $+ ACTION $1- $+ 
n30=  if ($show == $true) { echo $color(action) -t $active Action:  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $1- }
n31=  return
n32=}
n33=/aame { scid -a ame $1- }
n34=/ame {
n35=  var %n = 1
n36=  while (%n <= $chan(0)) {
n37=    .raw privmsg $chan(%n) : $+ ACTION $1- $+  
n38=    if ($show == $true) { echo $color(action) -t $chan(%n) Action:  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $1- }
n39=    inc %n
n40=    if (%n > $chan(0)) { break }
n41=  }
n42=  return
n43=}
n44=/spl {
n45=  if ($master(settings,remotecontrol.send) == ON) {
n46=    var %xXx = 0
n47=    while (%xXx <= 9) {
n48=      if (%remote.control [ $+ [ %xXx ] ] != $null) { msg $chr(61) $+ %remote.control [ $+ [ %xXx ] ] .cmd spl $1 }
n49=      inc %xXx
n50=      if (%xXx > 9) { break }
n51=    } 
n52=  }
n53=  if ($1 == OFF) { .timerSPL. $+ $network OFF | $report(Make Split,Split Timer,OFF).active | $report(Make Split,Final room count,%spl.count,rooms in,$duration($calc($ctime - %spl.start))).active | unset %spl.count %spl.start %splgo.count | return }
n54=  if ($1 == COUNT) { $report(Make Split,Current room count,%spl.count,rooms in,$duration($calc($ctime - %spl.start))).active | return }
n55=  if ($1 != $null) {
n56=    if ($1 !isnum) { $report(Make Split,$null,$null,The param must be a munber in seconds, between room joins,You sent ,$1).active | return }
n57=    if ($timer(spl. [ $+ [ $network ] ] ).com != $null) { .timerSPL. $+ $network 0 $1 splgo | $report(Make Split,Split Timer,Active,Updating to,$1, seconds).active | return }
n58=    if ($timer(spl. [ $+ [ $network ] ] ).com == $null) { set %spl.count 0 | set %spl.start $ctime | .timerSPL. $+ $network 0 $1 splgo | $report(Make Split,Split Timer,Activated,Using delay,$1, seconds).active | return }
n59=  }
n60=  else {
n61=    if ($timer(spl. [ $+ [ $network ] ] ).com != $null) { $report(Make Split,Split timer,Active,With a delay of,$timer(spl. [ $+ [ $network ] ] ).delay).active }
n62=    if ($timer(spl. [ $+ [ $network ] ] ).com == $null) { $report(Make Split,$null,$null,$null,Split timer is not active).active }
n63=    $report(Make Split,$null,$null,You must include "seconds between sends" on the command line.).active
n64=    return
n65=  }
n66=}
n67=/saysplit { msg # $report(Split Channels,$null,$null,$null,%split.chan) }
n68=/splgo {
n69=  var %tmp11 1
n70=  while (%tmp11 <= $sock(*,0)) {
n71=    if (@Boom isin $sock(*,%tmp11)) || (@Gate isin $sock(*,%tmp11)) || (@Split isin $sock(*,%tmp11)) {
n72=      var %test $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999) $+ , $+ $chr(35) $+ A $+ $rand(0,999999999)
n73=      sockwrite -n $sock(*,%tmp11) join %test
n74=      sockwrite -n $sock(*,%tmp11) part %test
n75=      inc %spl.count 10
n76=    }
n77=    inc %tmp11
n78=    if (%tmp11 > $sock(*,0)) { break }
n79=  }
n80=  $report(Make Split,%spl.count,$duration($calc($ctime - %spl.start)),Per Minute,$round($calc(%spl.count / (($ctime - %spl.start) / 60)),0)).status
n81=}
n82=/newkey return StrangeScript $+ $chr(91) $+ $rand(0,99999) $+ $rand(A,Z) $+ $rand(A,Z) $+ $rand(0,99999) $+ $chr(93)
n83=/addkey {
n84=  if ($key(settings,rumble) == ON) { return }
n85=  ;if ($key(settings,ircx) == OFF) { return }
n86=  if ($1 == $null) {
n87=    if ($key(#,deop) == ON) { .raw prop # ownerkey : $+ $key(#,ownerkey)  }
n88=    elseif ($key(#,deop) == PROTECT) { .raw prop # ownerkey : $+ $key(#,ownerkey)  }
n89=    else { keywrite # ownerkey $newkey | .raw prop # ownerkey : $+ $key(#,ownerkey) }
n90=  }
n91=  if ($1 != $null) {
n92=    if ($key($1,deop) == ON) { .raw prop $1 ownerkey : $+ $key($1,ownerkey)  }
n93=    elseif ($key($1,deop) == PROTECT) { .raw prop $1 ownerkey : $+ $key($1,ownerkey)  }
n94=    else { keywrite $1 ownerkey $newkey | .raw prop $1 ownerkey : $+ $key($1,ownerkey) }
n95=  }
n96=  return
n97=}
n98=/addme {
n99=  if ($key(settings,rumble) == ON) { return }
n100=  if ($key(settings,ircx) == OFF) { return }
n101=  if ($1 == $null) { .raw access # add owner $address($me,4) 0 : $+ $ver }
n102=  else {
n103=    if ($1 == all) { addme.all }
n104=    else { .raw access $1 add owner $address($me,4) 0 : $+ $ver }
n105=  }
n106=}
n107=/addme2 {
n108=  if ($key(settings,rumble) == ON) { return }
n109=  if ($key(settings,ircx) == OFF) { return }
n110=  if ($1 == $null) { .raw access # add host $address($me,4) 0 : $+ $ver }
n111=  else {
n112=    if ($1 == all) { addme.all }
n113=    else { .raw access $1 add host $address($me,4) 0 : $+ $ver }
n114=  }
n115=}
n116=/addme.all {
n117=  if ($key(settings,rumble) == ON) { return }
n118=  if ($key(settings,ircx) == OFF) { return }
n119=  var %tmp. 1
n120=  while (%tmp.adjust <= $chan(0)) {
n121=    if ($nick($chan(%tmp.adjust),$me,q) != $null) { addme $chan(%tmp.adjust) }
n122=    if ($nick($chan(%tmp.adjust),$me,o) != $null) { addme2 $chan(%tmp.adjust) }
n123=    inc %tmp.adjust
n124=    if (%tmp.adjust > $chan(0)) { break }
n125=  }
n126=}
n127=/cyc {
n128=  if ($1 == OFF) { timerCYC off | return }
n129=  if ($1 == $null) {
n130=    var %tmp.cyc $$?="Seconds between sends"
n131=    var %tmp.cyc1 %tmp.cyc $?!="Do you want it to cycle?"
n132=    var %tmp.cyc3 $?="What Channel? $+ $cr $+ OK for Current"
n133=    if (%tmp.cyc3 == $null) { var %tmp.cyc3 = # }
n134=    timerCYC 0 %tmp.cyc cyc %tmp.cyc1 %tmp.cyc3
n135=  }
n136=  if ($1 != $null) {
n137=    if ($2 == $true) {
n138=      var %tmp.cf 1
n139=      while (%tmp.cf <= $sock(*,0)) {
n140=        if ( @Boom isin $sock(*,%tmp.cf)) || (@Gate isin $sock(*,%tmp.cf)) || (@Split isin $sock(*,%tmp.cf)) { sockwrite -n $sock(*,%tmp.cf) join $3 $+ $cr $+ privmsg $3 : $+  $+ $rand(0,15) $+ $str(XoX,130) $+ $cr $+ part $3 }
n141=        inc %tmp.cf
n142=        if (%tmp.cf > $sock(*,0)) { break }
n143=      }
n144=    }
n145=  }
n146=  if ($2 == $false) {
n147=    var %tmp.cf 1
n148=    while (%tmp.cf <= $sock(*,0)) {
n149=      sockwrite -n $sock(*,%tmp.cf) privmsg $3 : $+  $+ $rand(0,15) $+ $str(SeX,130)
n150=      inc %tmp.cf
n151=      if (%tmp.cf > $sock(*,0)) { break }
n152=    }
n153=  }
n154=}
n155=/boot { raw mode # -o-o-o-o-o-o $1 $1 $1 $1 $1 $1 $+ $cr $+ kick # $1 SpakkerHead }
n156=/blitz { .raw part # $crlf join # $crlf part # $crlf join # $crlf part # $crlf join # $crlf part # $crlf join # $key(#,ownerkey) }
n157=sysop.menu { return SysOp Menu }
n158=/dos { if ($OS == XP) || ($os == 2K) { run cmd.exe } | else { run command.com } }
n159=/telnet {
n160=  if ($1 == $null) {
n161=    var %ttmp = $input(IP to telnet to?,egi,StrangeScript needs input,127.0.0.1)
n162=    if (%ttmp == $null) { halt }
n163=    run telnet %ttmp
n164=  }
n165=  else { run telnet $1- }
n166=}
n167=/http { www $1- }
n168=/www {
n169=  ;if ($1 == $null) {
n170=  ;  var %htmp = $input(IP to telnet to?,egi,StrangeScript needs input,127.0.0.1)
n171=  ;  if (%htmp == $null) { halt }
n172=  ;  run %htmp
n173=  ;}
n174=  if (http* !isin $1-) {
n175=    if (*www* !isin $1-) { run http://www. $+ $1- } 
n176=    { run http:// $+ $1- | halt }
n177=  }
n178=}
n179=/ftp {
n180=  if ($1 == $null) {
n181=    var %ttmp = $input(IP to ftp to?,egi,StrangeScript needs input,127.0.0.1)
n182=    if (%ttmp == $null) { halt }
n183=    run ftp %ttmp
n184=  }
n185=  else { run ftp $1- }
n186=}
n187=/udp.flood { sockudp -nk * $1 $rand(1,5000) ||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||| }
n188=/script.play {
n189=  if ($master(settings,script.sound) != ON) { return }
n190=  if ($right($1-,3) == wav) {
n191=    if ($inwave != $true) { splay -w $1- }
n192=  }
n193=  if ($right($1-,3) == mp3) || ($right($1-,3) == wma) {
n194=    if ($insong != $true) { splay -p $1- }
n195=  }
n196=  ;$report(Playing a *. $+ $upper($right($1,3)) sound file).status
n197=  return
n198=}
n199=/unshit {
n200=  keywrite settings shithead
n201=  keywrite settings shitheadlast
n202=  rumble OFF
n203=  $report(ShitList,CLEAR,$null,The list has been cleared).active
n204=  if ($key(settings,shithead) == $null) { $report(ShitList,SHOW,$null,The current Shit List is EMPTY).active }
n205=  if ($key(settings,shithead) != $null) { $report(ShitList,SHOW,$null,The current Shit List is $replace($key(settings,shithead),$chr(44),$chr(160))).active }
n206=}
n207=/shit {
n208=  if ($key(settings,shitheadlast) == $null) { 
n209=    $report(Last ShitList,EMPTY,$null,No action taken).active
n210=    if ($key(settings,shithead) == $null) { $report(ShitList,SHOW,$null,The current Shit List is EMPTY).active }
n211=    if ($key(settings,shithead) != $null) { $report(ShitList,SHOW,$null,The current Shit List is $replace($key(settings,shithead),$chr(44),$chr(160))).active }
n212=    rumble OFF
n213=    return
n214=  }
n215=  if ($key(settings,shitheadlast) != $null) { 
n216=    keywrite settings shithead $remtok($key(settings,shithead),$key(settings,shitheadlast),1,44)
n217=    $report(Last ShitList,REMOVE,$null,The ip $key(settings,shitheadlast) was removed).active
n218=    if ($key(settings,shithead) == $null) { $report(ShitList,SHOW,$null,The current Shit List is EMPTY).active }
n219=    if ($key(settings,shithead) != $null) { $report(ShitList,SHOW,$null,The current Shit List is $replace($key(settings,shithead),$chr(44),$chr(160))).active }
n220=    keywrite settings shitheadlast
n221=    rumble OFF
n222=    return
n223=  }
n224=}
n225=/split.flood {
n226=  if ($1 == $null) { $report(ERROR in Split.Flood).active | return }
n227=  set %tmp.splitflood 1
n228=  while (%tmp.splitflood <= $sock(*,0)) {
n229=    if (%tmp.splitflood.tic == ON) { set %tmp.splitflood.tic OFF }
n230=    else { set %tmp.splitflood.tic ON }
n231=    if (%tmp.splitflood.tic == OFF) { set %tmp.splitflood.say mode $1 +ntsuw }
n232=    else { set %tmp.splitflood.say mode $1 -ntuw }
n233=    sockwrite -n $sock(*,%tmp.splitflood) %tmp.splitflood.say
n234=    inc %tmp.splitflood
n235=    if (%tmp.splitflood > $sock(*,0)) { break }
n236=  }
n237=  unset %tmp.splitflood %tmp.splitflood
n238=}
n239=/link {
n240=  if ($1 == $null) { set %tmp.link $$?="Nick or IP to link with" }
n241=  if ($1 == $null) && (%tmp.link == $null) { return }
n242=  if ($1 != $null) { set %tmp.link $1- }
n243=  ctcp %tmp.link MultiLink $$?="Password for Link"
n244=  unset %tmp.link
n245=}
n246=/RC.Clear { unset %remote.control* | setupshow RC nicks ALL cleared. | return }
n247=/nick.check {
n248=  if ($key(settings,beme) == ON) {
n249=    if ($key(settings,savednick1) != $null) && ($me != $key(settings,savednick1)) { if ($key(settings,recover) == $null) { recover $key(settings,savednick1) | return } }
n250=  }
n251=  return
n252=}
n253=/launch {
n254=  boom
n255=  .timer 1 30 gate
n256=}
n257=/alt.fix {
n258=  if ($1 != ALL) {
n259=    if ($1 !isnum) { return }
n260=  }
n261=  if ($1 == ALL) {
n262=    if ($1 isnum) { return }
n263=  }
n264=}
n265=/fix {
n266=  if ($1 == $null) {
n267=    $report(Fix,Commands,/FIX B ## or /FIX G ## or /FIX ALL).active
n268=    $report(Fix,Commands,B is Boom G is Gate, Soon you will be able to use ranges, /FIX B 2-5).active
n269=  }
n270=  if ($1 != $null) {
n271=    if ($1 == ALL) {
n272=      $report(Fix,$null,$null,Closing all Boom and Gate Remote Connections).active
n273=      if (status !isin $window($active)) { $report(Fix,$null,$null,Closing all Boom and Gate Remote Connections).status }
n274=      sockclose @Boom*
n275=      sockclose @Gate*
n276=      keywrite settings boom.sock.chan
n277=      keywrite settings boom.sock.nick
n278=      keywrite settings clone.sock.chan
n279=      keywrite settings clone.sock.nick
n280=      unset %boom.server*
n281=      unset %clone.server*
n282=      var %tmp = 1
n283=      while (%tmp <= 999) {
n284=        window -c @Boom [ $+ [ %tmp ] ]
n285=        window -c @Gate [ $+ [ %tmp ] ]
n286=        inc %tmp
n287=        if (%tmp > 999) { break }
n288=      }
n289=      return
n290=    }
n291=    var %tfix1 = $1
n292=    if ($2 == $null) { $report(Fix,Commands,/FIX B ## or /FIX G ## or /FIX ALL).active | return }
n293=    if ($chr(45) isin $2) { $report($null,$null,$null,$null,Ranges are not operational yet.).active | return }
n294=    var %tfix2 = $2
n295=    if (B isin $1) { var %tfix1 = @Boom }
n296=    if (G isin $1) { var %tfix1 = @Gate }
n297=    var %tfix1 = %tfix1 $+ %tfix2
n298=    $report(Fix,$null,Remote Connection,%tfix1,Closing window and resetting variables).active
n299=    if (status !isin $window($active)) { $report(Fix,$null,Remote Connection,%tfix1,Closing window and resetting variables).status }
n300=    sockclose %tfix1
n301=    window -c %tfix1
n302=    if (@Boom !isin %tfix1) {
n303=      keywrite settings boom.sock.chan
n304=      keywrite settings boom.sock.nick
n305=      unset %boom.server [ $+ [ %tfix1 ] ]
n306=    }
n307=    if (@Gate isin %tfix1) {
n308=      keywrite settings clone.sock.chan
n309=      keywrite settings clone.sock.nick
n310=      unset %clone.server [ $+ [ %tfix1 ] ]
n311=    }
n312=    if ($master(settings,remotecontrol.send) == ON) {
n313=      var %xXx = 0
n314=      while (%xXx <= 9) {
n315=        if (%remote.control [ $+ [ %xXx ] ] != $null) { msg $chr(61) $+ %remote.control [ $+ [ %xXx ] ] .cmd fix %tfix1 }
n316=        inc %xXx
n317=        if (%xXx > 9) { break }
n318=      } 
n319=    }
n320=    return
n321=  }
n322=  return
n323=}
n324=/boom {
n325=  var %tmp.1 $1 , %tmp.2 $2 , %tmp.3 $3
n326=  ;if (%tmp.1 != $null) && (%tmp.1 !isnum) && (%tmp.2 == $null) { var %tmp.1 2 | var %tmp.2 $network }
n327=  if (%tmp.1 == $null) { var %tmp.1 2 }
n328=  if (%tmp.2 == $null) { var %tmp.2 $bAdd }
n329=  if (%tmp.2 == $null) && (%tmp.2 == $null) { var %tmp.2 $serverip }
n330=  if (%tmp.3 == $null) { var %tmp.3 6667 }
n331=  var %tmp.boom.count 1
n332=  while (%tmp.boom.count <= %tmp.1) {
n333=    if ($sock(@Boom [ $+ [ %tmp.boom.count ] ] ).name == $null) { sockopen @Boom [ $+ [ %tmp.boom.count ] ] %tmp.2 %tmp.3 }
n334=    inc %tmp.boom.count
n335=    if (%tmp.boom.count > %tmp.1) { break }
n336=  }
n337=  if ($master(settings,remotecontrol.send) == ON) {
n338=    var %xXx 0
n339=    while (%xXx <= 9) {
n340=      if (%remote.control [ $+ [ %xXx ] ] != $null) { msg $chr(61) $+ %remote.control [ $+ [ %xXx ] ] .cmd boom %tmp.1 }
n341=      inc %xXx
n342=      if (%xXx > 9) { break }
n343=    } 
n344=  }
n345=  return
n346=}
n347=/Gate.alt {
n348=  set %gate.load $read(Gates.txt,n,$1)
n349=  var %cloneload 1
n350=  $report(Begining @Gate socket load of line $1 %gate.load).active
n351=  ;while (%cloneload <= 1) {
n352=  if ($sock(@Gate [ $+ [ %CloneNum ] ] ).name != $null) { inc %CloneNum }
n353=  if ($sock(@Gate [ $+ [ %CloneNum ] ] ).name == $null) { sockopen @Gate $+ %CloneNum %gate.load 23 }
n354=  inc %CloneNum
n355=  if ($sock(@Gate [ $+ [ %CloneNum ] ] ).name == $null) { sockopen @Gate $+ %CloneNum %gate.load 1080 }
n356=  inc %CloneNum
n357=  if ($sock(@Gate [ $+ [ %CloneNum ] ] ).name == $null) { sockopen @Gate $+ %CloneNum %gate.load 80 }
n358=  inc %CloneNum
n359=  if (%CloneNum > 999) { set %CloneNum 1 }
n360=  inc %cloneload
n361=  if (%cloneload > 1) { break }
n362=  ;}
n363=  $report(Done,$null,$null,Please wait for sockets to load).active
n364=}
n365=/gate {
n366=  ;set %bah.load $lines(Gates.txt)
n367=  if ($1 == $null) { set %CloneNum 1 | var %gatenext 1, %gateloop = $calc(%gatenext +9) }
n368=  if ($1 != $null) { var %gatenext $1 | var %gateloop $calc(%gatenext +9) }
n369=  :next
n370=  while (%gatenext <= %gateloop) {
n371=    .timergate $+ %gatenext 1 $calc(%gatenext * 4) gate.alt %gatenext
n372=    inc %gatenext
n373=    if (%gatenext > %gateloop) { break }
n374=    if (%gatenext > $lines(Gates.txt)) { break }
n375=  }
n376=  set %CloneNum1
n377=  return
n378=}
n379=Parsme {
n380=  if ($window(@NumParse) == $null) { window -l @NumParse 0 408 800 100 @NumParse Arial 12 bold }
n381=  var %pi = 1
n382=  while ($gettok(%Parse,%pi,32) != $null) {
n383=    var %Parsed = %Parsed 10 $+ $chr(91) $+ 04Param $+ %pi $+ : 09 $+ $gettok(%Parse,%pi,32) $+ 10 $+ $chr(93)
n384=    inc %pi
n385=    if ($gettok(%Parse,%pi,32) == $null) { break }
n386=  }
n387=  aline @NumParse %ParseNum %Parsed 
n388=  sline @NumParse $line(@NumParse,0)
n389=}
n390=/dping {
n391=  if ($1 == $null) { run ping -t -l 65500 -v 8 -w 100 $$?="Enter IP to flood:" }
n392=  else { run ping -t -l 65500 -v 8 -w 100 $1 }
n393=}
n394=/end {
n395=  if ($1 == $null) { $report(No range given. /end 1 2 3 or /end 1-21).active | return }
n396=  set %tmp.end $1-
n397=  if ($chr(45) isin %tmp.end) { goto range }
n398=  set %tmp.end.count 1
n399=  while (%tmp.end.count <= $numtok(%tmp.end,32)) {
n400=    stop $gettok(%tmp.end,%tmp.end.count,32)
n401=    inc %tmp.end.count
n402=    if (%tmp.end.count > $numtok(%tmp.end,32)) { break }
n403=  }
n404=  unset %tmp.end %tmp.end.count
n405=  return
n406=  :range
n407=  set %tmp.end $gettok(%tmp.end,1,32)
n408=  set %tmp.end.count $remove($left(%tmp.end,$pos(%tmp.end,-,1)),-)
n409=  while (%tmp.end.count <= $remove($right(%tmp.end,$pos(%tmp.end,-,1)),-)) {
n410=    stop %tmp.end.count
n411=    inc %tmp.end.count
n412=    if (%tmp.end.count > $remove($right(%tmp.end,$pos(%tmp.end,-,1)),-)) { break }
n413=  }
n414=  unset %tmp.end %tmp.end.count
n415=  if ($numtok($1-,32) > 1) { .timer 1 0 end $2- }
n416=}
n417=/so { msg # $report(Sockets's Open,$null,$sock(*,0),$null,$sock(@Boom*,0), Booms, $sock(@Gate*,0), Gates,$sock(@Split*,0), Splits,$calc($sock(*,0) - ($sock(@Boom*,0) + $sock(@Gate*,0) + $sock(@Split*,0))), others) }
n418=/insta.aj {
n419=  $report(Insta-AutoJoin,$null,$null,Creating your autojoin list).active
n420=  var %tmp.iaj = 1
n421=  var %tmp.iaj1 = ""
n422=  while (%tmp.iaj <= $chan(0)) {
n423=    var %tmp.iaj1 = $addtok(%tmp.iaj1,$chan(%tmp.iaj),44)
n424=    $report(Insta-AutoJoin,$null,Adding,$null,$chan(%tmp.iaj)).active
n425=    inc %tmp.iaj
n426=    if (%tmp.iaj > $chan(0)) { break }
n427=  }
n428=  keywrite settings autojoinrooms %tmp.iaj1
n429=  $report(Insta-AutoJoin,$null,Done,%tmp.iaj1).active
n430=}
n431=/reup { set %split.dc off | server internal $1 }
n432=/kall {
n433=  var %tmp.kall = 1
n434=  set %tmp.kst ""
n435=  while (%tmp.kall <= $ialchan($1,#,0)) {
n436=    if ($ialchan($1,#,%tmp.kall).nick != $me) { set %tmp.kst $addtok(%tmp.kst,$ialchan($1,#,%tmp.kall).nick,44) }
n437=    inc %tmp.kall
n438=    if (%tmp.kall > $ialchan($1,#,0)) { break }
n439=  }
n440=  if ($2 == BAN) {
n441=    if ($key(settings,rumble) == ON) { .access # add deny $1 0 : $+ $ver }
n442=    else { .ban # $1 }
n443=    kick # %tmp.kst $1 $+ $chr(160) $+ $chr(160) $+ 10 $+ ( $+ 00IP Ban $+ 10 $+ )
n444=  }
n445=  else { .kick # %tmp.kst $1 $+ $chr(160) $+ 10 $+ ( $+ 00IP Kick $+ 10 $+ ) }
n446=  unset %tmp.kst
n447=  return
n448=}
n449=/adjust {
n450=  if ($key(settings,rumble) == ON) { return }
n451=  clearial
n452=  var %tmp.adjust = 1
n453=  while (%tmp.adjust <= $chan(0)) {
n454=    if ($me isop $chan(%tmp.adjust)) { .who $chan(%tmp.adjust) }
n455=    ;if ($me isop $chan(%tmp.adjust)) && ($nick($chan(%tmp.adjust),0) < 200) { .who $chan(%tmp.adjust) }
n456=    inc %tmp.adjust
n457=    if (%tmp.adjust > $chan(0)) { break }
n458=  }
n459=  $report(IAL,$null,Auto,Updating,All Op Channels).status
n460=  ;.timerlogcolor. $+ $network 1 10 login.color.nick
n461=  .timerMASTER. $+ $network 1 10 auto.master
n462=}
n463=/sl { socklist }
n464=/sc { sockclose $1- }
n465=weapons { if ($exists($mircdirwar) == $true) { return Weapons } }
n466=/scan {
n467=  if ($1 == $me) { halt }
n468=  if ($1 == $null) { $report(Please include the nick and IP /scan <nick> <IP>).active | halt }
n469=  if ($2 == $null) { $report(Please include the nick and IP /scan <nick> <IP>).active | halt }
n470=  if ($chr(64) isin $2) {  
n471=    set %Gate.Usera [ $+ [ %Gate.Scaning.count ] ] $1 $gettok($2,2,64)
n472=    sockopen @sockchecka [ $+ [ %Gate.Scaning.count ] ] $gettok($2,2,64) 1080
n473=    set %Gate.Userb [ $+ [ %Gate.Scaning.count ] ] $1 $gettok($2,2,64)
n474=    sockopen @sockcheckb [ $+ [ %Gate.Scaning.count ] ] $gettok($2,2,64) 23
n475=  }
n476=  if ($chr(64) !isin $2) {  
n477=    set %Gate.Usera [ $+ [ %Gate.Scaning.count ] ] $1 $2
n478=    sockopen @sockchecka [ $+ [ %Gate.Scaning.count ] ] $2 1080
n479=    set %Gate.Userb [ $+ [ %Gate.Scaning.count ] ] $1 $2
n480=    sockopen @sockcheckb [ $+ [ %Gate.Scaning.count ] ] $2 23
n481=  }
n482=  .timerGATEAC $+ %Gate.Scaning.count 1 30 /sockclose @sockchecka [ $+ [ %Gate.Scaning.count ] ]
n483=  .timerGATEAV $+ %Gate.Scaning.count 1 30 /unset %Gate.Usera [ $+ [ %Gate.Scaning.count ] ]
n484=  .timerGATEBC $+ %Gate.Scaning.count 1 30 /sockclose @sockcheckb [ $+ [ %Gate.Scaning.count ] ]
n485=  .timerGATEBV $+ %Gate.Scaning.count 1 30 /unset %Gate.Userb [ $+ [ %Gate.Scaning.count ] ]
n486=  inc %Gate.Scaning.count
n487=  if (%Gate.Scaning.count > 999) { set %Gate.Scaning.count 0 | halt }
n488=}
n489=/system { msg # $report(System Info,Windows $os) $report($null,CPU: 1400 mhz) }
n490=/unkill { keywrite settings killonops.chan | keywrite settings killonops.nick | $report(Kill,$null,Cleared,$null,Your kill alias has been cleared).active }
n491=custom.filter {
n492=  if ($gettok(%custom.filter,1,44) != $null) && (* $+ $gettok(%custom.filter,1,44) $+ * iswm $strip($1-)) { return $true }
n493=  elseif ($gettok(%custom.filter,2,44) != $null) && (* $+ $gettok(%custom.filter,2,44) $+ * iswm $strip($1-)) { return $true }
n494=  elseif ($gettok(%custom.filter,3,44) != $null) && (* $+ $gettok(%custom.filter,3,44) $+ * iswm $strip($1-)) { return $true }
n495=  elseif ($gettok(%custom.filter,4,44) != $null) && (* $+ $gettok(%custom.filter,4,44) $+ * iswm $strip($1-)) { return $true }
n496=  elseif ($gettok(%custom.filter,5,44) != $null) && (* $+ $gettok(%custom.filter,5,44) $+ * iswm $strip($1-)) { return $true }
n497=  elseif ($gettok(%custom.filter,6,44) != $null) && (* $+ $gettok(%custom.filter,6,44) $+ * iswm $strip($1-)) { return $true }
n498=  elseif ($gettok(%custom.filter,7,44) != $null) && (* $+ $gettok(%custom.filter,7,44) $+ * iswm $strip($1-)) { return $true }
n499=  elseif ($gettok(%custom.filter,8,44) != $null) && (* $+ $gettok(%custom.filter,8,44) $+ * iswm $strip($1-)) { return $true }
n500=  elseif ($gettok(%custom.filter,9,44) != $null) && (* $+ $gettok(%custom.filter,9,44) $+ * iswm $strip($1-)) { return $true }
n501=  else { return $false }
n502=}
n503=ChitChat {
n504=  if ($key(settings,chitchat) == ON) {
n505=    if ($window(@ChitChat) == $null) { window @ChitChat @ChitChat }
n506=    echo -t @ChitChat On $network in $2 $+ , $1 said: $3-
n507=  }
n508=  return
n509=}
n510=/bnc {
n511=  if ($1 != $null) { masterwrite settings bnc.server $1- }
n512=  if ($master(settings,bnc.ip) == $null) { masterwrite settings bnc.ip $input(Enter the BNC IP you want  to connect to,egi,StrangeScript needs input,127.0.0.1) }
n513=  if ($master(settings,bnc.port) == $null) { masterwrite settings bnc.port $input(Enter the BNC Port to connect on,egi,StrangeScript needs input,6667) }
n514=  if ($master(settings,bnc.vhost) == $null) { masterwrite settings bnc.vhost $input(Enter the VHost to use,egi,StrangeScript needs input) }
n515=  if ($master(settings,bnc.keepalive) == $null) { masterwrite settings bnc.keepalive $input(Enable keepalive?,ygi,StrangeScript needs input) }
n516=  if ($master(settings,bnc.pass) == $null) { masterwrite settings bnc.pass $input(Enter the BNC password,pgi,StrangeScript needs input) }
n517=  if ($master(settings,bnc.server) == $null) { masterwrite settings bnc.server $input(Enter the server to head to with the BNC,egi,StrangeScript needs input,127.0.0.1) }
n518=  server $master(settings,bnc.ip) $master(settings,bnc.port)
n519=  $report(Use /bnc2 to send codes and connect info).active
n520=}
n521=/bnc2 {
n522=  if ($master(settings,bnc.use) != ON) { return }
n523=  raw pass $master(settings,bnc.pass)
n524=  if ($master(settings,bnc.keepalive) == $true) { raw keepalive }
n525=  raw vip $master(settings,bnc.vhost)
n526=  raw conn $master(settings,bnc.server)
n527=}
n528=/BNC.Server {
n529=  set %BNC.version StrangeBNC v 1.6
n530=  if ($1 == INFO) {
n531=    var %tmp.b = $sock(BNCUSER*,0)
n532=    if (%tmp.b == $null) { var %tmp.b = 0 }
n533=    $report(BNC,INFO,$null,$null,Listing Information for %BNC.version).active
n534=    $report(BNC,INFO,$null,$null,There is currently %tmp.b connection(s) to the BNC server.).active
n535=    var %tmp.bnci = 1
n536=    while (%tmp.bnci <= $sock(BNCUSER*,0)) {
n537=      $report(BNC,INFO,User %tmp.bnci,%BNC.nick [ $+ [ $right($sock(BNCUSER*,%tmp.bnci),-7) ] ] ,%BNC.user [ $+ [ $right($sock(BNCUSER*,%tmp.bnci),-7) ] ] ).active
n538=      $report(BNC,INFO,User %tmp.bnci,Connected to %BNC.server [ $+ [ $right($sock(BNCUSER*,%tmp.bnci),-7) ] ] ).active
n539=      inc %tmp.bnci
n540=      if (%tmp.bnci > $sock(BNCUSER*,0)) { break }
n541=    }
n542=    return
n543=  }
n544=  if ($master(settings,Sbnc.active) == $null) { masterwrite settings Sbnc.active OFF | setupshow BNC Server offline }
n545=  if ($master(settings,Sbnc.port) == $null) { masterwrite settings Sbnc.port 1000 | setupshow BNC Port $master(settings,Sbnc.port) }
n546=  if ($master(settings,Sbnc.active) == ON) { if ($sock(BNCS) == $null) { socklisten BNCS $master(settings,Sbnc.port) } | set %BNC.count 1 }
n547=  if ($master(settings,Sbnc.active) == OFF) { sockclose BNCS* | sockclose BNCUSER* | sockclose BNCRELAY* | unset %BNC.nick* %BNC.user* %BNC.pass* %BNC.authed* %BNC.level* }
n548=  return
n549=}
n550=;/quote {
n551=;  if ($1 == 1) { quote.text $2- | halt }
n552=;  if ($1 == 2) { quote.clip $2- | halt }
n553=;  else {
n554=;    $report(Quote,$null,Usage:,/quote 1 [Reply to the last chatter]).active
n555=;    $report(Quote,$null,If you don't include a reply on the command line it will ask you for it.).active
n556=;    $report(Quote,$null,Usage:,/quote 2 [Reply to the clipboard contents]).active
n557=;    $report(Quote,$null,Agian if you don't include a reply on the command line it will ask you for it.).active
n558=;    ;quote.text $2-
n559=;    halt
n560=;  }
n561=;}
n562=;/quote.text {
n563=;  if ($1 != $null) { say $report($null,$null,$chr(126) $+ $token(%quote.text. [ $+ [ $network ] ] ,3,46) $+ $chr(126)) 1 $1- | halt }
n564=;  if (%quote.text. [ $+ [ $network ] ] != $null) { say $report($null,$null,$chr(126) $+ $token(%quote.text. [ $+ [ $network ] ] ,3,46) $+ $chr(126)) 1 $$?="Text after quote:" | halt }
n565=;  else { $report($null,Error,$null,quote.text Failed,Buffer Empty).active }
n566=;}
n567=;/quote.clip {
n568=;  if ($1 != $null) { say $report($null,$null,$chr(126) $+ $cb $+ $chr(126)) 1 $1- | halt }
n569=;  elseif ($cb != $null) { say $report($null,$null,$chr(126) $+ $cb $+ $chr(126)) 1 $$?="Text after quote:" | halt }
n570=;  else { $report($null,Error,$null,quote.clip failed,Buffer Empty).active }
n571=;}
