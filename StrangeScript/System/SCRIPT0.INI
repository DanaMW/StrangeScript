[script]
n0=;ON 1:*:*:{ $report($event,$rawmsg).active }
n1=; on 1:ACTIVE:*:{ $report(Activated: $active,De-Activated: $lactive).active | halt }
n2=on 1:LOGON:*:{ $report(LOGON Event triggered).active | halt }
n3=on 1:START: {
n4=  unset %mybar*
n5=  mygo
n6=}
n7=alias mygo  {
n8=  : this is set on the status/server menu
n9=  var %tmprs1 = 1
n10=  var %tmprs2 = $gettok($master(settings,autostartserver),0,44)
n11=  while (%tmprs1 <= %tmprs2) {
n12=    server -m $gettok($master(settings,autostartserver),%tmprs1,44)
n13=    inc %tmprs1
n14=    if (%tmprs1 > %tmprs2) { break }
n15=  }
n16=  unset %tmprs1
n17=  unset %tmprs2
n18=  return
n19=}
n20=;on ^*:SIGNAL:*: {
n21=;  halt 
n22=;  haldef
n23=;  ;$report(Signal,$network,$signal,$1-).active
n24=;  if ($signal == MYBAR) { mybar $network $key(settings,lagmrc) }
n25=;  halt
n26=;}
n27=on 1:CONNECT: {
n28=  if ($network != $null) {
n29=    var %tmp = $textdir $+ $network $+ .ini
n30=    if ($exists(%tmp) != $true) { .writeini -n %tmp settings boss $me }
n31=  }
n32=  server.joins.count
n33=  connect.setup
n34=  .timerBAR. $+ $network -om 0 500 mybar
n35=  keywrite settings ircx OFF
n36=  .timerIRCX. $+ $network 1 1 ircx
n37=  if ($key(settings,autoident) == ON) { auto.ident }
n38=  $report(Server,$null,$null,Set at,$network).active
n39=  set %setup.count 0
n40=  $report(AutoJoin,$null,$null,$null,$key(settings,autojoin)).status
n41=  if ($key(settings,autojoin) == ON)  { autojoin }
n42=  if ($key(settings,autojoin) == SPEED) { .raw join $key(settings,autojoinspeed) }
n43=  $report($fullver).status
n44=  lagon
n45=}
n46=on 1:CONNECTFAIL:{ $report(Connect Fail,$1-).active }
n47=on 1:DISCONNECT:{
n48=  if ($master(settings,script.sound) == ON) { script.play shit.wav }
n49=  set %split.prop ON
n50=  set %split.split OFF
n51=  if (%split.dc == ON) {
n52=    .timerSPLIT. $+ $network 1 0 split
n53=    set %split.dc OFF
n54=    if ($dialog(split) != $null) { did -c split 8 | did -u split 7 | did -u split 21 |  did -c split 12 }
n55=  }
n56=  if ($key(settings,savednick1) != $null) { nick $key(settings,savednick1) }
n57=  unset %mybar*
n58=  if (%ssl. [ $+ [ $network ] ] != $null) { unset %ssl. [ $+ [ $network ] ] }
n59=  ;splay stop
n60=  $report(Disconnect,$nick,$server,$time,$1-).status
n61=}
n62=on ^*:MODE:#: { 
n63=  haltdef
n64=  if ($key(settings,attempt.MODE) <= 5) {
n65=    ssflood MODE $nick
n66=    ;if ($master(settings,bnote) == ON) { $report(Mode,$null,$nick,$null,$1-).chan }
n67=    if ($master(settings,extra.info) == ON) { IF (status !isin $window($active)) { $report(Mode,$nick,$chan,$null,$1-).status } }
n68=  }
n69=  if (*+r* iswm $1-) { opall }
n70=  .timerMODEFIX. $+ $network 1 2 fix.modes # $nick $1- 
n71=}
n72=on *:OP:#:{  if ($opnick == $me) { .timerNAMES. $+ $chan $+ . $+ $network 1 1 hidden.rooms $chan } | return }
n73=on *:DEOP:#:{ return }
n74=on *:VOICE:#:{ return }
n75=on *:DEVOICE:#:{ return }
n76=on *:HELP:#:{ return }
n77=on *:DEHELP:#:{ return }
n78=on *:SERVEROP:#:{ return }
n79=on ^*:RAWMODE:#: {
n80=  haltdef
n81=  if ($key(settings,attempt.RAWMODE) <= 5) {
n82=    ssflood RAWMODE $nick
n83=    if ($master(settings,bnote) == ON) { $report(RawMode,$null,$nick,$null,$1-).chan }
n84=    if ($master(settings,extra.info) == ON) { IF (status !isin $window($active)) { $report(RawMode,$nick,$chan,$null,$1-).status } }
n85=  }
n86=  if ($nick == $server) { halt }
n87=  ;if ($nick == ChanServ) { halt }
n88=  ;if ($nick == ChanServ@services.dal.net) { halt }
n89=  if ($nick == System) { cycle | halt }
n90=  if ($nick == $me) && ($key(settings,addme.access) == ON) {
n91=    if (*-q* iswm $1) { sw all join # $key(#,ownerkey) }
n92=    if (*+q* iswm $1) && ($key(settings,ircx) == ON) { .timerMAKEMEQ. $+ $network $+ . $+ # 1 2 addme # }
n93=    if (*+o* iswm $1) && ($key(settings,ircx) == ON) { .timerMAKEMEOP. $+ $network $+ . $+ # 1 2 addme2 # }
n94=  }
n95=  if ($nick == $mode(1)) { halt }
n96=  if (*-o* iswm $1) && ($me isin $2-) { if ($chr(37) isin $chan(#)) { set $chan(#) $chan(#) } | if ($timer(DEQME. [ $+ [ $network ] ] ).com != $null) { halt } | if ($timer(DEQME. [ $+ [ $network ] ] ).com == $null) { .timerDEOPME. $+ $network 1 1 deop.protect.me $1 $nick $mode(1) $chan(#) $address($nick,2) } }
n97=  if (*-q* iswm $1) && ($me isin $2-) { if ($chr(37) isin $chan(#)) { set $chan(#) $chan(#) } | .timerDEQME. $+ $network -m 1 500 deop.protect.me $1 $nick $mode(1) $chan(#)  $address($nick,2) }
n98=  if (*-o* iswm $1) && ($me !isin $2-) { if ($chr(37) isin $chan(#)) { set $chan(#) $chan(#) } | if ($timer(DEQYOU. [ $+ [ $network ] ] ).com != $null) { halt } | if ($timer(DEQYOU. [ $+ [ $network ] ] ).com == $null) { .timerDEOPYOU. $+ $network 1 1 deop.protect.you $1 $nick $mode(1) $chan(#) $address($nick,2) } }
n99=  if (*-q* iswm $1) && ($me !isin $2-) { if ($chr(37) isin $chan(#)) { set $chan(#) $chan(#) } | .timerDEQYOU. $+ $network 1 1 deop.protect.you $1 $nick $mode(1) $chan(#)  $address($nick,2) }
n100=
n101=  if (*+o* iswm $1) && ($me isin $2-) {
n102=    .timerDEOPME. $+ $network off
n103=    .timerADJUST. $+ $network 1 30 adjust
n104=    if ($key(settings,killonops.chan) == $chan(#)) { mode $key(settings,killonops.chan) -o+b $key(settings,killonops.nick) $address($key(settings,killonops.nick),4) | kick $key(settings,killonops.chan) $key(settings,killonops.nick) KuntKick! | keywrite settings killonops.chan "" | keywrite settings killonops.nick "" }
n105=  }
n106=  if (*+q* iswm $1) && ($me isin $2-) && ($key(settings,ircx) == ON) {
n107=    .timerDEOPME. $+ $network off | .timerDEQME. $+ $network off | .timerMAKEKEY. $+ $network $+ . $+ # 1 2 addkey # | .timerADJUST. $+ $network 1 30 adjust
n108=    if ($key(settings,addme.access) == ON) { .timerMAKEMEQ 1 2 addme # }
n109=    if ($key(#,modemaster) == $null) { keywrite # modemaster OFF }
n110=    if ($key(#,deop) == $null) { keywrite # deop OFF }
n111=    if (*u* iswm $chan(#).mode) { names # }
n112=    if (*x* iswm $chan(#).mode) { names # }
n113=  }
n114=  if (*+o* iswm $1) && ($me !isin $2-) { .timerDEOPYOU. $+ $network off }
n115=  if (*+q* iswm $1) && ($me !isin $2-) { .timerDEOPYOU. $+ $network off | .timerDEQYOU. $+ $network off }
n116=  if (*+b* iswm $1) { banfart # $2 $nick | halt }
n117=  halt
n118=}
n119=alias bfircx {
n120=  ;mode $1 -bo+b $2 $3 $address($3,4)
n121=  mode $1 -b $2
n122=  mode $1 -o $3
n123=  ban $1 $address($3,4)
n124=  kick $1 $3 KuntKick!
n125=}
n126=alias banfart {
n127=  if ($3 == $me) { return }
n128=  if ($key(settings,ircx) == ON) { if ($2 iswm $address($me,5)) { bfircx $1 $2 $3 } | return }
n129=  else { if ($2 iswm $address($me,5)) { mode $1 -bo+b $2 $3 $address($3,4) | kick $1 $3 Out! } | return }
n130=}
n131=alias deop.protect.me {
n132=  $report(Deop protect me).active
n133=  if ($key(settings,deop.protect) == OFF) {
n134=    if ($key(settings,ircx) == ON) {
n135=      if ($network == COS) { chanserv OP $4 $me | halt }
n136=      else { .raw part $4 | .raw join $4 %key [ $+ [ $4 ] ] | halt }
n137=    }
n138=    if ($key(settings,ircx) == OFF) { chanserv OP $4 $me | halt }
n139=    halt
n140=  }
n141=  if ($2 == OpServ) { chanserv op $4 $me | return }
n142=  if ($2 == ChanServ) { chanserv OP $4 $me | halt }
n143=  .raw part $4 $crlf join $4 $key($4,ownerkey)
n144=  if ($sock(*).name == $null) { keywrite settings boom.sock.chan $4 | keywrite settings boom.sock.nick $2 | boom }
n145=  if ($sock(*).name != $null) { sw ALL join $4 $key($4,ownerkey) }
n146=  if ($key(settings,ircx) == ON) { .access $4 clear owner }
n147=  if ($5 != $address($me,2)) { .sw all mode $4 -o-o-o-o-o-o $ial($5,1).nick $ial($5,2).nick $ial($5,3).nick $ial($5,4).nick $ial($5,5).nick $ial($5,6).nick | sw all mode $4 -o-o-o-o-o-o  $ial($5,7).nick $ial($5,8).nick $ial($5,9).nick $ial($5,10).nick $ial($5,11).nick $ial($5,12).nick }
n148=  if ($key(settings,ircx) == ON) { sw all access $4 add owner $address($me,4) }
n149=  rumble on
n150=  if ($key(settings,ircx) == ON) {  sw all mode $4 +q $me }
n151=  if ($5 != $address($me,2)) { .raw mode $4 -o-o-o-o-o-o $ial($5,1).nick $ial($5,2).nick $ial($5,3).nick $ial($5,4).nick $ial($5,5).nick $ial($5,6).nick | .raw mode $4 -o-o-o-o-o-o  $ial($5,7).nick $ial($5,8).nick $ial($5,9).nick $ial($5,10).nick $ial($5,11).nick $ial($5,12).nick }
n152=  .kick $4 $2 Big mistake
n153=  keywrite settings killonops.nick $2 | keywrite settings killonops.chan $4
n154=  if ($key(settings,ircx) == ON) && ($key($4,deop) != PROTECT) { keywrite $4 ownerkey $newkey | prop $4 ownerkey $key($4,ownerkey) }
n155=  if ($address($2,4) != $address($me,4)) { keywrite settings shithead $addtok($key(settings,shithead),$address($2,4),44) | keywrite settings shitheadlast $address($2,4) }
n156=  .flood on
n157=  .timerMFlud. $+ $network 1 30 .flood off
n158=  if ($chr(37) isin $4) { unset $4 }
n159=  halt
n160=}
n161=alias deop.protect.you {
n162=  if ($ulist($address($3,3)) != $null) {
n163=    if ($key(settings,deop.protect) == OFF) { halt }
n164=    if ($key(settings,rumble) == ON) { halt }
n165=    var %tmp = $readini($AutoOwner,n,$4,$myaddress($3))
n166=    if (%tmp != $null) { .raw mode $4 +q $3 | unset %tmp | if ($chr(37) isin $4) { unset $4 } | halt }
n167=    set %tmp $readini($AutoOp,n,$4,$myaddress($3))
n168=    if (%tmp != $null) { .raw mode $4 +o $3 | unset %tmp | if ($chr(37) isin $4) { unset $4 } | halt }
n169=    if ($chr(37) isin $4) { unset $4 }
n170=    halt
n171=  }
n172=  var %tp = 1
n173=  while (%tp <= $sock(@boom*,0).mark) {
n174=    if ($3 == $sock(@boom*,%tp).mark) { var %oomm = $sock(@boom*,%tp).mark | var %oomn = $right($sock(@boom*,%tp),-5) | break }
n175=    inc %tp
n176=    if (%tp > $sock(@boom*,0).mark) { break }
n177=  }
n178=  if ($3 == %oomm) {
n179=    if ($2 == $me) { 
n180=      sw %oomn part $4 $crlf join $4 $key($4,ownerkey)
n181=      sw %oomn privmsg $4 : $+ $master(settings,socksay)
n182=      halt
n183=    }
n184=    if ($2 != $me) {
n185=      sw %oomn part $4 $crlf join $4 $key($4,ownerkey)
n186=      if ($istok($key(settings,botnick),$2,44) == $true) { sw %oomn privmsg $4 :Fucking Bot |  halt }
n187=      else { if ($address($2,4) != $address($me,4)) { sw all access $4 add host $2 $+ !*@* | sw %oomn mode $4 -o $2 | halt } }
n188=    }
n189=  }
n190=  if ($key($4,kicklock) == ON) {
n191=    if ($2 isowner $chan($4)) { halt }
n192=    if ($1 == -o) { .raw mode $4 -o+o $2 $3 | $report($2 just deoped $3 in $4 and was punished).active  }
n193=    if ($1 == -q) { .raw mode $4 -o+q $2 $3 | $report($2 just deQ'd $3 in $4 and was punished).active  }
n194=    .msg $4 10 $+ $chr(91) $+ $bright $+ System  Warning $+ 10 $+ $chr(93) $+ : $lowcol $+ If you deop or Deq YOU will be de-op'd
n195=    if ($chr(37) isin $4) { unset $4 }
n196=    halt
n197=  }
n198=  if ($chr(37) isin $4) { unset $4 }
n199=}
n200=on ^*:JOIN:#: { 
n201=  haltdef
n202=  if ($key(settings,attempt.JOIN) <= 5) {
n203=    ssflood JOIN $nick
n204=    if ($master(settings,bnote) == ON) { $report(Join,$null,$nick,$address).chan }
n205=    if ($master(settings,extra.info) == ON) { IF (status !isin $window($active)) { $report(Join,$chan,$nick,$address).status } }
n206=  }
n207=  if ($shit.check($nick) == $true) {
n208=    .kick # $nick 04ShitListed
n209=    if ($key(settings,bancheck) == ON) {
n210=      if ($key(settings,ircx) == ON) && ($nick(#,$me,q) != $null) { .access # add deny $address($nick,4) 0 : $+ $ver }
n211=      if ($key(settings,ircx) == OFF) && ($nick(#,$me,o) != $null) { .ban # $address($nick,4) }
n212=    }
n213=  }
n214=  if ($nick != $me) {
n215=    keywrite # lastin $nick
n216=    else { keywrite # lastinIP $myaddress2($nick) }
n217=    if ($key(settings,greet) == ON) && ($me isop $chan(#)) { .timerGREET. $+ $network 1 1 msg $nick $read($textdir $+ greet.txt,1) }
n218=  }
n219=  if ($key(settings,autovoice) == ON) && ($key(settings,rumble) != ON) { if (*m* iswm $chan(#).mode) && ($me isop $chan(#)) { .mode # +v $nick } }
n220=  if ($key(#,GateScan) == ON) { if ($key(settings,rumble) == OFF) && ($address($nick,4) !isin $key(settings,shithead)) { if ($me isop $chan(#)) { if (%Gate.Scaning.Count == $null) { set %Gate.Scaning.Count 0 } | /scan $nick $address } } }
n221=  .timerlogcolor. $+ $network 1 10 login.color.nick
n222=  .timerADJUST. $+ $network 1 30 adjust
n223=  if ($nick == $me) {
n224=    set %speed. $+ # $ticks
n225=    if ($key(settings,ircx) == ON) { .timerQME. $+ $network 1 1 sw ALL mode # +q $me }
n226=    else { .timerOME. $+ $network 1 1 sw ALL mode # +o $me }
n227=    remjoins $chan
n228=    joincheck $chan $nick
n229=    check.room.vars
n230=    if ($network == Libera.chat) { cs op # $me }
n231=    if ($sock(Bot*) != $null) { sockwrite -n Bot* mode # +o $me }
n232=    if (%split.dc == ON) && ($sock(@Split*) != $null) { .timerS. $+ $network 1 1 sockwrite -n @Split* mode # +q $me }
n233=    if ($key(settings,ircx) == ON) && ($key(settings,addme.access) == ON) { if ($key(settings,rumble) != ON) { .timerMAKMEQ. $+ $network 1 3 addme # } }
n234=  }
n235=  if ($key(settings,auto.socketop) == ON) {
n236=    if ($sock(@boom*) != $null) {
n237=      var %tp = 1
n238=      while (%tp <= $sock(@boom*,0).mark) {
n239=        if ($nick == $sock(@boom*,%tp).mark) { var %oomm = $sock(@boom*,%tp).mark | var %oomn = $sock(@boom*,%tp) | break }
n240=        inc %tp
n241=        if (%tp > $sock(@boom*,0).mark) { break }
n242=      }
n243=      if (%oomm != $null) && ($sock(%oomn).status == active) {
n244=        if ($key(settings,ircx) == ON) && ($nick(#,$me,q) != $null) { .raw mode # +q %oomm | $report(Socket,$null,$null,Auto-Qing Socket %oomm).chan }
n245=        if ($key(settings,ircx) == ON) && ($nick(#,$me,o) != $null) { .raw mode # +o %oomm | $report(Socket,$null,$null,Auto-Oping Socket %oomm).chan }
n246=        if ($key(settings,ircx) == OFF) && ($me isop $chan(#)) { .raw mode # +o %oomm | $report(Socket,$null,$null,Auto-Oping Socket %oomm).chan }
n247=        return
n248=      }
n249=    }
n250=    if ($sock(@Gate*) != $null) {
n251=      var %tp = 1
n252=      while (%tp <= $sock(@Gate*,0).mark) {
n253=        if ($nick == $sock(@Gate*,%tp).mark) { var %oomm = $sock(@Gate*,%tp).mark | var %oomn = $sock(@Gate*,%tp) | break }
n254=        inc %tp
n255=        if (%tp > $sock(@Gate*,0).mark) { break }
n256=      }
n257=      if (%oomm != $null) && ($sock(%oomn).status == active) {
n258=        if ($key(settings,ircx) == ON) && ($nick(#,$me,q) != $null) { .raw mode # +q %oomm | $report(Socket,$null,$null,Auto-Qing Socket %oomm).chan }
n259=        if ($key(settings,ircx) == ON) && ($nick(#,$me,o) != $null) { .raw mode # +o %oomm | $report(Socket,$null,$null,Auto-Oping Socket %oomm).chan }
n260=        if ($key(settings,ircx) == OFF) && ($me isop $chan(#)) { .raw mode # +o %oomm | $report(Socket,$null,$null,Auto-Oping Socket %oomm).chan }
n261=        return
n262=      }
n263=    }
n264=  }
n265=  if ($key(settings,auto.botop) == ON) {
n266=    if ($istok($key(settings,botnick),$nick,44) == $true) && ($nick != $me) {
n267=      if ($key(settings,ircx) == ON) && ($nick(#,$me,q) != $null) { .raw mode # +q $nick | $report(Bot,#,Auto-Q,ON).chan }
n268=      if ($key(settings,ircx) == ON) && ($nick(#,$me,o) != $null) { .raw mode # +o $nick | $report(Bot,#,Auto-Op,ON).chan }
n269=      if ($key(settings,ircx) == OFF) && ($me isop $chan(#)) { .raw mode # +o $nick | $report(Bot,#,Auto-Op,ON).chan }
n270=    }
n271=  }
n272=  if ($level($address($nick,3)) == 4) { if ($key(settings,autoops) == on) && ($key(settings,rumble) == OFF) { auto.op # $nick +q Owner } }
n273=  if ($level($address($nick,3)) == 3) { 
n274=    if ($key(settings,autoops) == on) && ($key(settings,rumble) == OFF) {
n275=      var %tmp $readini($AutoOwner,n,#,$myaddress($nick))
n276=      if (%tmp != $null) { auto.op # $nick +q Owner | halt }   
n277=      var %tmp = $readini($AutoOp,n,#,$myaddress($nick))
n278=      if (%tmp != $null) { auto.op # $nick +o Op | halt }   
n279=      else { auto.op.not # $nick 3 | halt }
n280=    }
n281=    else { auto.op.rumble # $nick 3 | halt }
n282=  }
n283=  halt
n284=}
n285=raw 366:*: {
n286=  if ($me ison $2) && (%speed. [ $+ [ $2 ] ] != $null) {
n287=    echo -t $2 $sys $report(Speed,Join,$2,Synched in:,$calc(($ticks - %speed. [ $+ [ $2 ] ] ) / 1000), secs)
n288=    unset %speed. $+ $2
n289=    ;unset %speed. $+ #
n290=    cline %sc.me $2 $me
n291=  }
n292=  return
n293=}
n294=on *:INPUT:*: {
n295=  if ($1 == $null) { $report(Input,No text sent).active | halt }
n296=  if (@Boom* iswm $target) { halt }
n297=  if (@SSC* iswm $target) { halt }
n298=  if (@Black* iswm $target) { halt }
n299=  if (@MiniSer* iswm $target) { halt }
n300=  if ($1 == //.msg) { set %tmp.quiet on }
n301=  if ($1 == /.msg) { set %tmp.quiet on }
n302=  if ($1 == .msg) { set %tmp.quiet on }
n303=  if ($master(settings,spell.check) == ON) { tokenize 32 $spell.check($1-) }
n304=  if ($left($1,1) == $chr(33)) && ($ctrlenter != $true) { do.socks $1- | halt }
n305=  ;if ($1 == $chr(126)) && ($ctrlenter != $true) { splitsay $2- }
n306=  if ($mouse.key & 4) {
n307=    if ($master(settings,strange.talker) == NORMAL) { say $1- | halt }
n308=    if ($master(settings,strange.talker) == RAND) { col $1- | halt }
n309=    if ($master(settings,strange.talker) == MIX) { mix $1- | halt }
n310=    if ($master(settings,strange.talker) == COLOR) { say  $+ %sc.text $+ $1- | halt }
n311=    if ($master(settings,strange.talker) == FREAK) { freak $1- | halt }
n312=    if ($master(settings,strange.talker) == TWIST) { twist $1- | halt }
n313=    if (.* !iswm $1) && ($master(settings,strange.talker) == SIMPLE) { dana $1- | halt }
n314=    if (.* !iswm $1) && ($master(settings,strange.talker) == SPOT) { spot $1- | halt }
n315=  }
n316=  if (/* !iswm $1) {
n317=    if ($ctrlenter != $true) {
n318=      if (%sc.text.active == MIX) { mix $1- | halt }
n319=      if (%sc.text.active == RAND) { col $1- | halt }
n320=      if (%sc.text.active == FREAK) { freak $1- | halt }
n321=      if (%sc.text.active == TWIST) { twist $1- | halt }
n322=      if (.* !iswm $1) && (%sc.text.active == SIMPLE) { dana $1- | halt }
n323=      if (.* !iswm $1) && (%sc.text.active == SPOT) { spot $1- | halt }
n324=      if (%sc.text.active == COLOR) && (%sc.text != $null) { say  $+ %sc.text $+ $1- | halt }
n325=      if (%sc.text.active == NORMAL) { say  $1- | halt }
n326=    }
n327=    if ($ctrlenter == $true) { if ($inpaste != $true) { hex.out $1- | halt } | else { say $1- | halt } }
n328=  }
n329=  if (/* iswm $1) && ($ctrlenter == $true) { say $1- | halt }
n330=}
n331=on *:INPUT:=: {
n332=  if ($1 == $null) { $report(Input,No text sent).active | halt }
n333=  if (/* !iswm $1) { say $1- | return }
n334=}
n335=on ^*:TEXT:*:#: {
n336=  haltdef
n337=  set %quote.text. [ $+ [ $network ] ] $chan $+ . $+ $nick $+ . $+ $1-
n338=  if ($chr(35) isin $1) {
n339=    if (Appears isin $2) { halt }
n340=    if (HeresInfo: isin $2) { halt }
n341=  }
n342=  if (*Breaking News* iswm $strip($1-)) && ($network == COS) { script.play smash.wav }
n343=  if ($chr(35) isin $nick) && ($key(settings,onjoinpart.show) == OFF) { halt }
n344=  if ($left($1,2) == ) { unhex.out $nick # $right($1-,-2) | halt }
n345=  if ($1- = up) && ($level($address($nick,3)) == 3) {
n346=    $report(AutoOp Request,$nick,#,Requested and received auto op's).active
n347=    if ($key(settings,ircx) == ON) { .raw mode # +q $nick }
n348=    if ($key(settings,ircx) == OFF) { .raw mode # +o $nick }
n349=  }
n350=  if ($1- = up) && ($level($address($nick,3)) == 4) {
n351=    $report(AutoOp Request,$nick,#,Requested and received auto op's).active
n352=    if ($key(settings,ircx) == ON) { .raw mode # +q $nick }
n353=    if ($key(settings,ircx) == OFF) { .raw mode # +o $nick }
n354=  }
n355=  if ($1 == setup) && ($level($address($nick,3)) >= 3) { repeat $nick $chan | unshit |  .timerSET. $+ $network 1 15 setprop $chan JOIN }
n356=  if ($left($1,1) == $chr(33)) && ($level($address($nick,3)) == 4) { do.socks $1- | halt }
n357=  if ($1- isnum) { goto nohighlight }
n358=  if ($custom.filter($1-) == $true) { halt }
n359=  if ($highlight($1-) != $null) {
n360=    if ($master(settings,HOP.active) == ON) && ($editbox($active) == $null) { .window -a $chan(#) }
n361=    ChitChat $nick # $1-
n362=  }
n363=  if ($highlight == $true) { goto highlighting }
n364=  :nohighlight
n365=  if ($level($address($nick,3)) == 5) && ($nick != $me) { echo -t # $output  $+ %sc.me $+ $lll $+ $white $nick  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n366=  if ($istok($key(settings,botnick),$nick,44) == $true) { echo -t # $output  $+ %sc.bot $+ $lll $+ $white $nick  $+ %sc.bot $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n367=  if ($level($address($nick,3)) == 4) { echo -t # $output  $+ %sc.me $+ $lll $+ $white $nick  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n368=  if ($level($address($nick,3)) == 3) { echo -t # $output  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n369=  if ($level($address($nick,3)) == 2) { echo -t # $output  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n370=  if ($level($address($nick,3)) >= 1) { echo -t # $output  $+ 14 $+ $lll $+ $white $nick  $+ 14 $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n371=  goto bail
n372=  :highlighting
n373=  if ($level($address($nick,3)) == 5) && ($nick != $me) { echo -lt # $output  $+ %sc.me $+ $lll $+ $white $nick  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n374=  if ($istok($key(settings,botnick),$nick,44) == $true) { echo -lt # $output  $+ %sc.bot $+ $lll $+ $white $nick  $+ %sc.bot $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n375=  if ($level($address($nick,3)) == 4) { echo -lt # $output  $+ %sc.me $+ $lll $+ $white $nick  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n376=  if ($level($address($nick,3)) == 3) { echo -lt # $output  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n377=  if ($level($address($nick,3)) == 2) { echo -lt # $output  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n378=  if ($level($address($nick,3)) >= 1) { echo -lt # $output  $+ 14 $+ $lll $+ $white $nick  $+ 14 $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n379=  :bail
n380=  halt
n381=}
n382=on ^*:ACTION:*:#: {
n383=  haltdef
n384=  if ($key(settings,action.hear) != ON) { halt }
n385=  if ($custom.filter($1-) == $true) { halt }
n386=  if ($key(settings,attempt.ACTION) <= 5) {
n387=    ssflood ACTION $nick
n388=    if ($highlight($1-) != $null) { ChitChat $nick # Action $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n389=    if ($highlight == $true) && ($1- != 1) { goto highlighting }
n390=    if ($level($address($nick,3)) == 5) && ($nick != $me) { echo $color(action) -t # Action:  $+ %sc.me $+ $lll $+ $white $nick  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n391=    if ($istok($key(settings,botnick),$nick,44) == $true) { echo $color(action) -t # Action:  $+ %sc.bot $+ $lll $+ $white $nick  $+ %sc.bot $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n392=    if ($level($address($nick,3)) == 4) { echo $color(action) -t # Action:  $+ %sc.me $+ $lll $+ $white $nick  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n393=    if ($level($address($nick,3)) == 3) { echo $color(action) -t # Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n394=    if ($level($address($nick,3)) == 2) { echo $color(action) -t # Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n395=    if ($level($address($nick,3)) >= 1) { echo $color(action) -t # Action:  $+ 14 $+ $lll $+ $white $nick  $+ 14 $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n396=    halt
n397=    :highlighting
n398=    if ($level($address($nick,3)) == 5) && ($nick != $me) { echo $color(action) -lt # Action:  $+ %sc.me $+ $lll $+ $white $nick  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n399=    if ($istok($key(settings,botnick),$nick,44) == $true) { echo $color(action) -lt # Action:  $+ %sc.bot $+ $lll $+ $white $nick  $+ %sc.bot $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n400=    if ($level($address($nick,3)) == 4) { echo $color(action) -lt # Action:  $+ %sc.me $+ $lll $+ $white $nick  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n401=    if ($level($address($nick,3)) == 3) { echo $color(action) -lt # Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n402=    if ($level($address($nick,3)) == 2) { echo $color(action) -lt # Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n403=    if ($level($address($nick,3)) >= 1) { echo $color(action) -lt # Action:  $+ 14 $+ $lll $+ $white $nick  $+ 14 $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n404=    halt
n405=  }
n406=  :bail
n407=  halt
n408=}
n409=on ^*:ACTION:*:?: {
n410=  haltdef
n411=  if ($key(settings,action.hear) != ON) { 
n412=    .write -a $logdiraway.log $date(mm/dd/yyyy) $time(hh:nn:ss-TT) $network $nick Actioned: $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-)
n413=    .notice $nick $report(Actions,OFF, $me is not accepting Actions.,Auto-Ignore,Not Active)
n414=    ;.ignore -pu $+ $master(settings,ignore.length) $nick
n415=    halt
n416=  }
n417=  if ($key(settings,attempt.ACTION) <= 5) {
n418=    ssflood ACTION $nick
n419=    if ($key(settings,away.iam) == ON) { write -a $logdiraway.log $date(mm/dd/yyyy) $time(hh:nn:ss-TT) $network $nick Actioned: $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | halt }
n420=    if ($highlight($1-) != $null) { ChitChat $nick $server Action $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n421=    if ($highlight == $true) { goto highlighting }
n422=    if ($master(settings,Query.Windows) == ON) {
n423=      if ($window($nick) == $null) { /query $nick }
n424=      if ($level($address($nick,3)) == 5) && ($nick != $me) { echo $color(action) -t $nick Action:  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n425=      if ($istok($key(settings,botnick),$nick,44) == $true) { echo $color(action) -t $nick Action:  $+ %sc.bot $+ $lll $+ $white $nick  $+ %sc.bot $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n426=      if ($level($address($nick,3)) == 4) { echo $color(action) -t $nick Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n427=      if ($level($address($nick,3)) == 3) { echo $color(action) -t $nick Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n428=      if ($level($address($nick,3)) == 2) { echo $color(action) -t $nick Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n429=      if ($level($address($nick,3)) >= 1) { echo $color(action) -t $nick Action: 14 $+ $lll $+ $white $nick 14 $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n430=      halt
n431=    }
n432=    else {
n433=      close -m
n434=      if ($level($address($nick,3)) == 5) && ($nick != $me) { echo $color(action) -at Action $+ $net $+ :  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n435=      if ($istok($key(settings,botnick),$nick,44) == $true) { echo $color(action) -at Action $+ $net $+ :  $+ %sc.bot $+ $lll $+ $white $nick  $+ %sc.bot $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n436=      if ($level($address($nick,3)) == 4) { echo $color(action) -at Action $+ $net $+ :  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n437=      if ($level($address($nick,3)) == 3) { echo $color(action) -at Action $+ $net $+ :  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n438=      if ($level($address($nick,3)) == 2) { echo $color(action) -at Action $+ $net $+ :  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n439=      if ($level($address($nick,3)) >= 1) { echo $color(action) -at Action $+ $net $+ : 14 $+ $lll $+ $white $nick 14 $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n440=      halt 
n441=    }
n442=    halt
n443=    :highlighting
n444=    if ($master(settings,Query.Windows) == ON) {
n445=      if ($window($nick) == $null) { /query $nick }
n446=      if ($level($address($nick,3)) == 5) { echo $color(action) -lt $nick Action:  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n447=      if ($istok($key(settings,botnick),$nick,44) == $true) { echo $color(action) -lt $nick Action:  $+ %sc.bot $+ $lll $+ $white $nick  $+ %sc.bot $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n448=      if ($level($address($nick,3)) == 4) { echo $color(action) -lt $nick Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n449=      if ($level($address($nick,3)) == 3) { echo $color(action) -lt $nick Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n450=      if ($level($address($nick,3)) == 2) { echo $color(action) -lt $nick Action:  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n451=      if ($level($address($nick,3)) >= 1) { echo $color(action) -lt $nick Action: 14 $+ $lll $+ $white $nick 14 $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n452=      halt
n453=    }
n454=    else { 
n455=      close -m
n456=      if ($level($address($nick,3)) == 5) { echo $color(action) -alt Action $+ $net $+ :  $+ %sc.me $+ $lll $+ $white $me  $+ %sc.me $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n457=      if ($istok($key(settings,botnick),$nick,44) == $true) { echo $color(action) -alt Action $+ $net $+ :  $+ %sc.bot $+ $lll $+ $white $nick  $+ %sc.bot $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) | goto bail }
n458=      if ($level($address($nick,3)) == 4) { echo $color(action) -alt Action $+ $net $+ :  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n459=      if ($level($address($nick,3)) == 3) { echo $color(action) -alt Action $+ $net $+ :  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n460=      if ($level($address($nick,3)) == 2) { echo $color(action) -alt Action $+ $net $+ :  $+ %sc.friend $+ $lll $+ $white $nick  $+ %sc.friend $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n461=      if ($level($address($nick,3)) >= 1) { echo $color(action) -alt Action $+ $net $+ : 14 $+ $lll $+ $white $nick 14 $+ $rrr $+  $iif($chr(40) $+ $chr(35) $+ * $+ $chr(41) iswm $1,$2-,$1-) }
n462=      halt
n463=    }
n464=  }
n465=  :bail
n466=  halt
n467=}
